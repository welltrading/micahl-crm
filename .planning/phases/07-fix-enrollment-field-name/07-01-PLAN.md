---
phase: 07-fix-enrollment-field-name
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/lib/airtable/contacts.ts
  - src/lib/airtable/__tests__/contacts.test.ts
autonomous: true
requirements:
  - CONT-02
must_haves:
  truths:
    - "getContactEnrollments returns real enrollment records (not empty array) when a contact has enrollments in Airtable"
    - "getEnrolledContactIds returns actual contact IDs from all enrollment records"
    - "npm test passes cleanly with no failures in contacts.test.ts"
  artifacts:
    - path: "src/lib/airtable/contacts.ts"
      provides: "Fixed enrollment queries using correct Airtable field name"
      contains: "איש קשר"
    - path: "src/lib/airtable/__tests__/contacts.test.ts"
      provides: "Aligned test assertions matching production code"
      contains: "נרשמות"
  key_links:
    - from: "src/lib/airtable/contacts.ts"
      to: "Airtable נרשמות table"
      via: "FIND+ARRAYJOIN filterByFormula"
      pattern: "FIND.*ARRAYJOIN.*איש קשר"
    - from: "src/lib/airtable/__tests__/contacts.test.ts"
      to: "contacts.ts getContactEnrollments"
      via: "mockTable assertion"
      pattern: "toHaveBeenCalledWith.*נרשמות"
---

<objective>
Fix the critical field name bug in contacts.ts that causes getContactEnrollments and getEnrolledContactIds to return empty results in production, then align test mocks to match production code.

Purpose: CONT-02 — מיכל יכולה לראות לכל איש קשר אילו הודעות קיבל — is blocked by this bug. The נרשמות table field linking contacts is named `איש קשר` in Airtable, but contacts.ts queries `אשת קשר` (4 locations), returning no results.

Output: Corrected contacts.ts with 4 field name fixes, and contacts.test.ts with 2 mock fixes, all tests green.
</objective>

<execution_context>
@C:/Users/londi/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/londi/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
</context>

<interfaces>
<!-- Key interfaces the executor needs. Extracted from source files. -->

From src/lib/airtable/contacts.ts (lines 78–137) — current broken state, exact fixes listed below:

```typescript
// getContactEnrollments — BROKEN at lines 81, 88:
filterByFormula: `FIND("${contactId}", ARRAYJOIN({אשת קשר}))`,  // line 81 — wrong field
contact_id: r.fields['אשת קשר'] as string[],                    // line 88 — wrong field

// getEnrolledContactIds — BROKEN at lines 126, 131:
.select({ fields: ['אשת קשר'] })                               // line 126 — wrong field
const contactIds = r.fields['אשת קשר'] as string[] | undefined; // line 131 — wrong field
```

From src/lib/airtable/__tests__/contacts.test.ts — current broken state, exact fixes:

```typescript
// Line 131 — getContactById mock missing _rawJson:
mockFind.mockResolvedValueOnce({
  id: 'recXYZ',
  fields: { 'שם פרטי': 'test', 'שם משפחה': '', 'שם מלא': 'test', 'טלפון': '972500000000' },
});

// Line 216 — wrong table name assertion:
expect(mockTable).toHaveBeenCalledWith('CampaignEnrollments');
```

Reference — getContactMessages (lines 98–118) uses CORRECT field name `איש קשר` — match this pattern.
Reference — scheduler-services.ts uses CONTACT_FIELD = 'איש קשר' — same correct name.
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Fix 4 field name occurrences in contacts.ts</name>
  <files>src/lib/airtable/contacts.ts</files>
  <action>
    Replace all 4 occurrences of `אשת קשר` with `איש קשר` in contacts.ts. These are the exact 4 locations:

    1. getContactEnrollments, filterByFormula string (line ~81):
       BEFORE: `FIND("${contactId}", ARRAYJOIN({אשת קשר}))`
       AFTER:  `FIND("${contactId}", ARRAYJOIN({איש קשר}))`

    2. getContactEnrollments, field read in map() (line ~88):
       BEFORE: `contact_id: r.fields['אשת קשר'] as string[]`
       AFTER:  `contact_id: r.fields['איש קשר'] as string[]`

    3. getEnrolledContactIds, .select() fields array (line ~126):
       BEFORE: `.select({ fields: ['אשת קשר'] })`
       AFTER:  `.select({ fields: ['איש קשר'] })`

    4. getEnrolledContactIds, field read in loop (line ~131):
       BEFORE: `const contactIds = r.fields['אשת קשר'] as string[] | undefined`
       AFTER:  `const contactIds = r.fields['איש קשר'] as string[] | undefined`

    Do NOT change the table name `נרשמות` — that is the correct Airtable table name.
    Do NOT change any other function (getContactMessages already uses correct `איש קשר`).
    Do NOT change comments or documentation strings — only fix the 4 field name literals.
    Do NOT add a `mapEnrollmentSource` offset label export — it is intentionally private.
  </action>
  <verify>
    <automated>cd '/d/CLAUDE CODE HERE/MICAHL CRM' && grep -n "אשת קשר" src/lib/airtable/contacts.ts && echo "FAIL: still has wrong field" || echo "PASS: no אשת קשר found"</automated>
  </verify>
  <done>Zero occurrences of `אשת קשר` remain in contacts.ts. The 4 locations all read `איש קשר`. Table name `נרשמות` is untouched.</done>
</task>

<task type="auto">
  <name>Task 2: Fix 2 test mock issues in contacts.test.ts</name>
  <files>src/lib/airtable/__tests__/contacts.test.ts</files>
  <action>
    Make two targeted fixes to contacts.test.ts:

    FIX 1 — Line ~131: Add `_rawJson` to the getContactById mock that tests "calls Airtable find with the correct record id". The mock object is missing `_rawJson.createdTime` which production code reads via `record._rawJson.createdTime`.

    BEFORE:
    ```
    mockFind.mockResolvedValueOnce({
      id: 'recXYZ',
      fields: { 'שם פרטי': 'test', 'שם משפחה': '', 'שם מלא': 'test', 'טלפון': '972500000000' },
    });
    ```
    AFTER:
    ```
    mockFind.mockResolvedValueOnce({
      id: 'recXYZ',
      _rawJson: { createdTime: '2026-03-15T00:00:00.000Z' },
      fields: { 'שם פרטי': 'test', 'שם משפחה': '', 'שם מלא': 'test', 'טלפון': '972500000000' },
    });
    ```

    FIX 2 — Line ~216: Update the stale table name assertion in "queries the CampaignEnrollments table" test. Production code calls `airtableBase('נרשמות')`, but the test asserts `'CampaignEnrollments'`.

    BEFORE:
    ```
    expect(mockTable).toHaveBeenCalledWith('CampaignEnrollments');
    ```
    AFTER:
    ```
    expect(mockTable).toHaveBeenCalledWith('נרשמות');
    ```

    No other changes. Do not rename the test description string ("queries the CampaignEnrollments table") — only fix the assertion value.

    After both fixes, run the test to confirm green:
    `npm test -- --testPathPattern=contacts.test`
  </action>
  <verify>
    <automated>cd '/d/CLAUDE CODE HERE/MICAHL CRM' && npm test -- --testPathPattern=contacts.test --passWithNoTests 2>&1 | tail -20</automated>
  </verify>
  <done>npm test -- --testPathPattern=contacts.test exits 0. All tests in contacts.test.ts pass. No `אשת קשר` assertions remain.</done>
</task>

</tasks>

<verification>
Full contacts test suite green:
`npm test -- --testPathPattern=contacts.test`

Zero wrong field name occurrences remain:
`grep -rn "אשת קשר" src/lib/airtable/` must return empty.

Full test suite check:
`npm test` — all tests green before plan completion.
</verification>

<success_criteria>
- All 4 `אשת קשר` → `איש קשר` fixes applied in contacts.ts
- contacts.test.ts line ~131 mock has `_rawJson: { createdTime: '...' }`
- contacts.test.ts line ~216 asserts `'נרשמות'` not `'CampaignEnrollments'`
- `npm test -- --testPathPattern=contacts.test` exits 0 with all tests green
- `npm test` full suite exits 0
</success_criteria>

<output>
After completion, create `.planning/phases/07-fix-enrollment-field-name/07-01-SUMMARY.md` with:
- What was fixed (4 locations in contacts.ts, 2 mocks in contacts.test.ts)
- Verification result (test output)
- Any unexpected findings during the audit
</output>
