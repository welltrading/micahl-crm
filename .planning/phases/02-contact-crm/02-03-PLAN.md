---
phase: 02-contact-crm
plan: 03
type: execute
wave: 2
depends_on:
  - 02-01
files_modified:
  - src/app/anshei-kesher/page.tsx
  - src/app/anshei-kesher/actions.ts
  - src/app/anshei-kesher/__tests__/actions.test.ts
  - src/components/contacts/ContactsPageClient.tsx
  - src/components/contacts/ContactsTable.tsx
  - src/components/contacts/AddContactModal.tsx
autonomous: false
requirements:
  - CONT-01
  - CONT-03
  - UX-01
must_haves:
  truths:
    - "מיכל פותחת /anshei-kesher ורואה טבלת אנשי קשר עם עמודות: שם מלא, טלפון (050-XXX-XXXX), תאריך הצטרפות, קמפיין"
    - "שדה חיפוש מסנן שורות לפי שם או טלפון בזמן אמת — ללא טעינה מחדש"
    - "3 כרטיסי סטטיסטיקה מעל הטבלה: סך אנשי קשר, הצטרפו החודש, הצטרפו השבוע"
    - "לחיצה על 'הוסף איש קשר' פותחת מודל עם שדות שם מלא + טלפון"
    - "שליחת טלפון כפול מציגה הודעת שגיאה בעברית: 'המספר כבר קיים במערכת'"
    - "איש קשר חדש שנוסף מופיע ברשימה לאחר רענון (revalidatePath)"
  artifacts:
    - path: "src/app/anshei-kesher/page.tsx"
      provides: "Server component fetching all contacts"
    - path: "src/app/anshei-kesher/actions.ts"
      provides: "addContact Server Action"
      exports: ["addContact"]
    - path: "src/components/contacts/ContactsPageClient.tsx"
      provides: "Client component owning search, modal, sheet state"
    - path: "src/components/contacts/ContactsTable.tsx"
      provides: "Pure display table receiving filtered contacts"
    - path: "src/components/contacts/AddContactModal.tsx"
      provides: "Dialog modal for adding contact"
  key_links:
    - from: "src/app/anshei-kesher/page.tsx"
      to: "src/lib/airtable/contacts.ts getContacts()"
      via: "direct import in server component"
      pattern: "getContacts"
    - from: "src/components/contacts/AddContactModal.tsx"
      to: "src/app/anshei-kesher/actions.ts addContact()"
      via: "Server Action import in client component"
      pattern: "addContact"
    - from: "src/app/anshei-kesher/actions.ts"
      to: "src/lib/airtable/phone.ts normalizePhone()"
      via: "import and call before duplicate check"
      pattern: "normalizePhone"
---

<objective>
Build the Contact CRM list page: stat cards, searchable table, and add-contact modal. All data operations go through Server Components and Server Actions — Michal never touches Airtable directly.

Purpose: Delivers CONT-01 (visible contact list), CONT-03 (stats), and UX-01 (all actions via UI).
Output: Full contacts page at `/anshei-kesher` with Hebrew RTL layout, stat cards, search, table, and working add-contact modal.
</objective>

<execution_context>
@C:/Users/londi/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/londi/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/02-contact-crm/02-CONTEXT.md
@.planning/phases/02-contact-crm/02-RESEARCH.md
@.planning/phases/02-contact-crm/02-01-SUMMARY.md

<interfaces>
<!-- Existing types and components — executor uses directly -->

From src/lib/airtable/types.ts:
```typescript
export interface Contact {
  id: string;
  full_name: string;
  phone: string; // normalized: 972XXXXXXXXXX
  joined_date?: string;
  notes?: string;
  created_at: string;
}
```

From src/lib/airtable/contacts.ts (available after 02-01):
```typescript
export async function getContacts(): Promise<Contact[]>
export async function createContact(fullName: string, phone: string): Promise<void>
// Note: this is a service function, NOT a Server Action.
// The Server Action wrapper lives in src/app/anshei-kesher/actions.ts
```

From src/lib/airtable/phone.ts (created in 02-01):
```typescript
export function normalizePhone(raw: string): string
export function formatPhoneDisplay(normalized: string): string
// formatPhoneDisplay('972501234567') → '050-123-4567'
```

From src/components/ui/sheet.tsx (existing):
```typescript
// Sheet, SheetContent, SheetHeader, SheetTitle — slide panel, uses @base-ui/react/dialog
```

From src/components/ui/card.tsx (existing):
```typescript
// Card, CardHeader, CardTitle, CardContent — use for stat cards
```

From src/components/ui/button.tsx (existing):
```typescript
// Button — use for "הוסף איש קשר" trigger
```

RTL layout rules (confirmed from Phase 1):
- dir="rtl" is set on <html> — flex-row puts items right-to-left automatically
- DO NOT use flex-row-reverse — it double-reverses
- Tailwind 4 logical properties: ms-* (margin-start), ps-* (padding-start), border-s
- First <th>/<td> in JSX = rightmost visual column in RTL table
  Column JSX order (rightmost to leftmost): שם מלא | טלפון | תאריך הצטרפות | קמפיין
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: addContact Server Action with duplicate check</name>
  <files>
    src/app/anshei-kesher/actions.ts,
    src/app/anshei-kesher/__tests__/actions.test.ts
  </files>
  <behavior>
    - Test: addContact with a phone already in contacts list → returns `{ error: 'המספר כבר קיים במערכת' }`
    - Test: duplicate check normalizes both incoming and existing phones before comparing (050-123-4567 matches 972501234567)
    - Test: addContact with valid unique phone → calls createContact, calls revalidatePath('/anshei-kesher'), returns `{ success: true }`
    - Test: addContact with empty fullName → returns `{ error: 'שם מלא נדרש' }`
  </behavior>
  <action>
    Create `src/app/anshei-kesher/actions.ts` with `'use server'` at top.

    ```typescript
    'use server';
    import { revalidatePath } from 'next/cache';
    import { getContacts, createContact } from '@/lib/airtable/contacts';
    import { normalizePhone } from '@/lib/airtable/phone';

    export async function addContact(
      fullName: string,
      phone: string
    ): Promise<{ success: true } | { error: string }> {
      if (!fullName?.trim()) return { error: 'שם מלא נדרש' };
      const normalized = normalizePhone(phone);
      const existing = await getContacts();
      const duplicate = existing.find((c) => normalizePhone(c.phone) === normalized);
      if (duplicate) return { error: 'המספר כבר קיים במערכת' };
      await createContact(fullName.trim(), normalized);
      revalidatePath('/anshei-kesher');
      return { success: true };
    }
    ```

    Write tests first (RED). Mock `getContacts`, `createContact`, `revalidatePath`, `normalizePhone` in tests.
    Confirm duplicate check normalizes BOTH sides — this is a known pitfall from RESEARCH.md.
  </action>
  <verify>
    <automated>npm test -- --testPathPattern="actions"</automated>
  </verify>
  <done>All 4 tests pass. `npm run build` clean.</done>
</task>

<task type="auto">
  <name>Task 2: Contacts page — server component + client UI + add modal</name>
  <files>
    src/app/anshei-kesher/page.tsx,
    src/components/contacts/ContactsPageClient.tsx,
    src/components/contacts/ContactsTable.tsx,
    src/components/contacts/AddContactModal.tsx
  </files>
  <action>
    This is the server/client split described in RESEARCH.md Pattern 1.

    **src/app/anshei-kesher/page.tsx** (server component):
    - Import getContacts from '@/lib/airtable/contacts'
    - Fetch contacts and pass to ContactsPageClient
    - Add `export const dynamic = 'force-dynamic'` to prevent caching (contacts change via webhook)

    **src/components/contacts/ContactsPageClient.tsx** ('use client'):
    - Props: `{ contacts: Contact[] }`
    - State: search string, addModalOpen boolean, selectedContact (used in plan 02-04)
    - Derive stats from contacts array (no extra fetch):
      - total: contacts.length
      - this month: contacts where created_at is in current calendar month
      - this week: contacts where created_at is within last 7 days (millisecond comparison)
    - Render: 3 stat cards (Card/CardHeader/CardContent), search input, "הוסף איש קשר" button, ContactsTable, AddContactModal
    - Search filters on full_name (includes) OR phone (includes, strip dashes from search term)
    - Pass filtered contacts to ContactsTable
    - Pass setSelectedContact to ContactsTable for row clicks (panel opens in plan 02-04)

    **src/components/contacts/ContactsTable.tsx** (no 'use client' needed if it receives callbacks as props — but it needs onClick, so mark 'use client'):
    - Props: `{ contacts: Contact[]; onContactClick: (c: Contact) => void }`
    - Render: `<table>` with Hebrew column headers
    - Column JSX order (= RTL display order right-to-left): שם מלא | טלפון | תאריך הצטרפות | קמפיין
    - Display phone using formatPhoneDisplay(contact.phone) — 972XXXXXXXXXX → 050-XXX-XXXX
    - Display joined_date: format as Hebrew date (use toLocaleDateString('he-IL'))
    - Row onClick → calls onContactClick(contact)
    - Empty state: if no contacts, show centered Hebrew message "אין אנשי קשר עדיין"
    - Hover state on rows: `hover:bg-muted/50 cursor-pointer`

    **src/components/contacts/AddContactModal.tsx** ('use client'):
    - Props: `{ open: boolean; onOpenChange: (o: boolean) => void }`
    - Uses @base-ui/react/dialog (same primitive as Sheet.tsx — check sheet.tsx for import pattern)
    - Form fields: שם מלא (text input, required) + טלפון (tel input, required)
    - On submit: call addContact(fullName, phone) Server Action, show Hebrew error if returned, close modal and router.refresh() on success
    - While submitting: disable button, show "שומר..." text
    - Hebrew labels and placeholder text throughout

    Do NOT use flex-row-reverse anywhere. Trust dir="rtl" on html.
  </action>
  <verify>
    <automated>npm run build</automated>
  </verify>
  <done>
    Build succeeds with no TypeScript errors. `/anshei-kesher` page renders. Stat cards, search input, table with correct RTL column order, and "הוסף איש קשר" button all present.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Checkpoint: Verify contacts page UI and add-contact flow</name>
  <what-built>
    Contacts page at /anshei-kesher with: 3 stat cards (total, this month, this week), Hebrew RTL table (שם מלא | טלפון | תאריך הצטרפות | קמפיין), search box, and "הוסף איש קשר" modal.
  </what-built>
  <how-to-verify>
    1. Visit http://localhost:3000/anshei-kesher (run `npm run dev` first)
    2. Confirm: 3 stat cards render at the top with correct Hebrew labels
    3. Confirm: Table renders with RTL column order (שם מלא rightmost, קמפיין leftmost)
    4. Confirm: Phone numbers display in 050-XXX-XXXX format (not 972XXXXXXXXXX)
    5. Confirm: Search box filters by name and phone in real-time without page reload
    6. Click "הוסף איש קשר" — confirm modal opens with שם מלא and טלפון fields
    7. Submit with a duplicate phone number — confirm Hebrew error "המספר כבר קיים במערכת" appears
    8. Submit with valid new contact — confirm modal closes and contact appears after refresh
  </how-to-verify>
  <resume-signal>Type "approved" or describe any visual/functional issues</resume-signal>
  <action>Human verifies the contacts page UI, search, modal, and add-contact flow as described in how-to-verify.</action>
  <done>Michal confirms: stat cards render, table shows RTL columns, search works, add modal blocks duplicates with Hebrew error.</done>
</task>

</tasks>

<verification>
- `npm test -- --testPathPattern="actions"` passes (addContact duplicate check)
- `npm run build` succeeds
- Visual verification: contacts page renders correctly in RTL with correct column order
</verification>

<success_criteria>
- Contact list visible at /anshei-kesher without opening Airtable (CONT-01, UX-01)
- 3 stat cards showing correct totals (CONT-03)
- Search filters by name or phone client-side
- Add contact modal blocks duplicate phones with Hebrew error
- New contacts appear after page refresh
</success_criteria>

<output>
After completion, create `.planning/phases/02-contact-crm/02-03-SUMMARY.md`
</output>
