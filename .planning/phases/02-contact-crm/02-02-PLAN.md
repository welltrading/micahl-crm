---
phase: 02-contact-crm
plan: 02
type: execute
wave: 2
depends_on:
  - 02-01
files_modified:
  - src/app/api/webhook/contact/route.ts
  - src/app/api/webhook/contact/__tests__/route.test.ts
  - .env.local
autonomous: true
requirements:
  - INFRA-04
must_haves:
  truths:
    - "POST /api/webhook/contact returns 401 when x-webhook-secret header is missing or wrong"
    - "POST /api/webhook/contact returns 400 when full_name or phone is missing from body"
    - "POST /api/webhook/contact returns 201 and creates an Airtable contact when request is valid"
    - "Phone is normalized to 972XXXXXXXXXX before Airtable write"
    - "WEBHOOK_SECRET is defined in .env.local"
  artifacts:
    - path: "src/app/api/webhook/contact/route.ts"
      provides: "POST Route Handler for MAKE.com webhook"
      exports: ["POST"]
    - path: "src/app/api/webhook/contact/__tests__/route.test.ts"
      provides: "Unit tests for webhook auth, validation, phone normalization"
    - path: ".env.local"
      provides: "WEBHOOK_SECRET env var"
      contains: "WEBHOOK_SECRET="
  key_links:
    - from: "src/app/api/webhook/contact/route.ts"
      to: "src/lib/airtable/phone.ts normalizePhone()"
      via: "import and call before airtableBase write"
      pattern: "normalizePhone"
    - from: "src/app/api/webhook/contact/route.ts"
      to: "process.env.WEBHOOK_SECRET"
      via: "x-webhook-secret header check"
      pattern: "WEBHOOK_SECRET"
---

<objective>
Build the MAKE.com webhook endpoint that silently creates new contacts from external registrations.

Purpose: MAKE.com sends a POST when a new person signs up. This endpoint normalizes the phone and creates the Airtable record. Michal sees the contact on next page refresh.
Output: `src/app/api/webhook/contact/route.ts` (authenticated POST handler), unit tests, `WEBHOOK_SECRET` in `.env.local`.
</objective>

<execution_context>
@C:/Users/londi/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/londi/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/02-contact-crm/02-CONTEXT.md
@.planning/phases/02-contact-crm/02-RESEARCH.md
@.planning/phases/02-contact-crm/02-01-SUMMARY.md

<interfaces>
<!-- From 02-01 output — executor should use these directly -->

From src/lib/airtable/phone.ts (created in 02-01):
```typescript
export function normalizePhone(raw: string): string
```

From src/lib/airtable/client.ts:
```typescript
export const airtableBase: (tableName: string) => Airtable.Table<Airtable.FieldSet>
// NOTE: airtableBase imports 'server-only' — safe to use in Route Handlers (server-side)
```

Expected MAKE.com payload shape (document in the file header comment):
```json
{
  "full_name": "string (required)",
  "phone": "string (required)",
  "campaign": "string (optional, Airtable campaign record ID if enrolling)"
}
```

Airtable field names for Contacts table:
- 'שם מלא' — full name
- 'טלפון' — phone (store normalized 972XXXXXXXXXX)
- 'תאריך הצטרפות' — join date (ISO date string YYYY-MM-DD)
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Webhook route handler with auth + validation</name>
  <files>
    src/app/api/webhook/contact/route.ts,
    src/app/api/webhook/contact/__tests__/route.test.ts,
    .env.local
  </files>
  <behavior>
    - Test: POST without x-webhook-secret header → 401 `{ error: 'Unauthorized' }`
    - Test: POST with wrong secret value → 401 `{ error: 'Unauthorized' }`
    - Test: POST with correct secret but missing full_name → 400 `{ error: 'Missing required fields' }`
    - Test: POST with correct secret but missing phone → 400 `{ error: 'Missing required fields' }`
    - Test: POST with valid secret + valid body → calls airtableBase('Contacts').create with normalized phone → 201 `{ success: true }`
    - Test: POST with phone '050-123-4567' → stored as '972501234567' (normalizePhone called)
  </behavior>
  <action>
    1. Generate a WEBHOOK_SECRET: run `openssl rand -hex 32` and add the result to `.env.local` as `WEBHOOK_SECRET=<value>`. Also add `WEBHOOK_SECRET=` to `.env.local.example` with an empty value.

    2. Write the test file first (RED). Mock `airtableBase` and `normalizePhone`. Use `process.env.WEBHOOK_SECRET = 'test-secret'` in beforeEach for tests.

    3. Implement `src/app/api/webhook/contact/route.ts`:
       - `export async function POST(req: NextRequest)`
       - Check `req.headers.get('x-webhook-secret')` against `process.env.WEBHOOK_SECRET` — return 401 if mismatch
       - Parse body JSON, validate `full_name` and `phone` present — return 400 if missing
       - Call `normalizePhone(phone)`
       - Call `airtableBase('Contacts').create({ 'שם מלא': full_name, 'טלפון': normalized, 'תאריך הצטרפות': today })` where today = `new Date().toISOString().split('T')[0]`
       - Return `NextResponse.json({ success: true }, { status: 201 })`
       - Add a file-level comment documenting the expected MAKE.com payload shape (see interfaces above)

    Authentication method: `x-webhook-secret` header (not URL param — keeps secret out of server logs).

    Important: Do NOT call `revalidatePath` here — webhook runs server-side without a user session, and revalidation is not needed (contacts page shows up-to-date on next user refresh, per CONTEXT.md decision).
  </action>
  <verify>
    <automated>npm test -- --testPathPattern="webhook"</automated>
  </verify>
  <done>
    All 6 test cases pass. Route file exists. WEBHOOK_SECRET in .env.local. `npm run build` clean.
  </done>
</task>

</tasks>

<verification>
- `npm test -- --testPathPattern="webhook"` passes
- `npm run build` produces no TypeScript errors
- `.env.local` contains WEBHOOK_SECRET
- `.env.local.example` contains WEBHOOK_SECRET= (empty, for documentation)
</verification>

<success_criteria>
- Webhook endpoint rejects unauthenticated requests with 401
- Webhook endpoint validates required fields with 400
- Valid requests create normalized Airtable contacts and return 201
- All tests green, no TypeScript errors
</success_criteria>

<output>
After completion, create `.planning/phases/02-contact-crm/02-02-SUMMARY.md`
</output>
