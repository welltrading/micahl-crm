---
phase: 01-foundation
plan: 03
type: execute
wave: 2
depends_on:
  - 01-01
  - 01-02
files_modified:
  - src/lib/airtable/client.ts
  - src/lib/airtable/campaigns.ts
  - src/lib/airtable/contacts.ts
  - src/lib/airtable/types.ts
  - src/app/layout.tsx
  - src/components/layout/Sidebar.tsx
  - src/components/layout/MobileHeader.tsx
  - src/app/page.tsx
  - src/app/kampanim/page.tsx
  - src/app/anshei-kesher/page.tsx
  - src/app/hagdarot/page.tsx
autonomous: false
requirements:
  - INFRA-03
  - UX-03

must_haves:
  truths:
    - "All Airtable calls are server-side only — the Airtable token never appears in the browser Network tab"
    - "The dashboard loads at / with Hebrew RTL layout — sidebar on the right, all text right-aligned"
    - "Sidebar has 4 navigation items in Hebrew: תפריט, קמפיינים, אנשי קשר, הגדרות"
    - "Campaigns and Contacts pages show a Hebrew placeholder 'בקרוב' message"
    - "Settings page has step-by-step guides for connecting GREEN API and configuring MAKE.com webhook"
    - "On mobile, the sidebar is hidden behind a hamburger menu that opens as an overlay"
  artifacts:
    - path: "src/lib/airtable/client.ts"
      provides: "Server-only Airtable client singleton — throws at import if used in a client component"
      exports: ["airtableBase"]
    - path: "src/lib/airtable/types.ts"
      provides: "TypeScript interfaces for all 5 Airtable table record shapes"
      exports: ["Campaign", "Contact", "CampaignEnrollment", "ScheduledMessage", "MessageLog"]
    - path: "src/lib/airtable/campaigns.ts"
      provides: "Server-side functions to read/write Campaigns table"
      exports: ["getCampaigns", "getCampaignById"]
    - path: "src/lib/airtable/contacts.ts"
      provides: "Server-side functions to read/write Contacts table"
      exports: ["getContacts", "getContactById"]
    - path: "src/components/layout/Sidebar.tsx"
      provides: "Hebrew RTL sidebar with navigation links and mobile overlay behavior"
    - path: "src/app/hagdarot/page.tsx"
      provides: "Settings page with GREEN API and MAKE.com step-by-step guides"
  key_links:
    - from: "src/lib/airtable/client.ts"
      to: "process.env.AIRTABLE_API_TOKEN"
      via: "server-only import guard"
      pattern: "server-only|AIRTABLE_API_TOKEN"
    - from: "src/lib/airtable/campaigns.ts"
      to: "src/lib/airtable/client.ts"
      via: "named import of airtableBase"
      pattern: "import.*airtableBase"
    - from: "src/app/layout.tsx"
      to: "src/components/layout/Sidebar.tsx"
      via: "JSX include in root layout"
      pattern: "<Sidebar"
---

<objective>
Build the server-only Airtable service layer and the Hebrew RTL dashboard layout with navigation sidebar and Settings page.

Purpose: INFRA-03 requires that no Airtable call ever reaches the browser — the service layer enforces this. UX-03 requires Michal can self-configure integrations using in-app guides.
Output: A working dashboard layout with Hebrew RTL navigation, a Settings page with integration guides, and a typed server-only service layer ready for Phase 2 to extend.
</objective>

<execution_context>
@C:/Users/londi/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/londi/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/01-foundation/01-CONTEXT.md
@.planning/phases/01-foundation/01-01-SUMMARY.md
@.planning/phases/01-foundation/01-02-SUMMARY.md

<interfaces>
<!-- Field API names from Plan 02 schema. Executor: use these exactly — do not invent field names. -->
<!-- If 01-02-SUMMARY.md documents any changes to field names, those take precedence. -->

Campaigns table fields: campaign_name, event_date, description, status, created_at
Contacts table fields: full_name, phone, joined_date, notes, created_at
CampaignEnrollments table fields: campaign_id, contact_id, enrolled_at, source
ScheduledMessages table fields: campaign_id, contact_id, message_content, send_at, offset_label, status, sent_at
MessageLog table fields: scheduled_message_id, contact_id, campaign_id, status, green_api_response, error_message, logged_at

Status values (ScheduledMessages.status): pending | sending | sent | failed
Status values (Campaigns.status): future | active | ended
Offset labels: week_before | day_before | morning | half_hour
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Server-only Airtable service layer</name>
  <files>src/lib/airtable/client.ts, src/lib/airtable/types.ts, src/lib/airtable/campaigns.ts, src/lib/airtable/contacts.ts, src/lib/airtable/__tests__/campaigns.test.ts</files>
  <behavior>
    - getCampaigns() returns an array of Campaign objects — never throws on empty table, returns []
    - getCampaignById(id) returns Campaign or null if not found
    - getContacts() returns Contact[] — never throws on empty table
    - getContactById(id) returns Contact or null
    - Importing client.ts in a test environment with no AIRTABLE_API_TOKEN set throws a clear error: "AIRTABLE_API_TOKEN is not set — Airtable client must be used server-side only"
    - All return types are typed — no `any`
  </behavior>
  <action>
    **`src/lib/airtable/client.ts`** — Server-only guard + singleton:
    ```typescript
    import 'server-only'; // Next.js package — throws if imported in client component
    import Airtable from 'airtable';

    if (!process.env.AIRTABLE_API_TOKEN) {
      throw new Error('AIRTABLE_API_TOKEN is not set — Airtable client must be used server-side only');
    }
    if (!process.env.AIRTABLE_BASE_ID) {
      throw new Error('AIRTABLE_BASE_ID is not set');
    }

    export const airtableBase = new Airtable({
      apiKey: process.env.AIRTABLE_API_TOKEN,
    }).base(process.env.AIRTABLE_BASE_ID);
    ```

    Install `server-only` package: `npm install server-only`

    **`src/lib/airtable/types.ts`** — TypeScript interfaces matching the schema:
    ```typescript
    export interface Campaign {
      id: string;
      campaign_name: string;
      event_date: string; // ISO8601 UTC
      description?: string;
      status: 'future' | 'active' | 'ended';
      created_at: string;
    }

    export interface Contact {
      id: string;
      full_name: string;
      phone: string; // normalized: 972XXXXXXXXXX
      joined_date?: string;
      notes?: string;
      created_at: string;
    }

    export interface CampaignEnrollment {
      id: string;
      campaign_id: string[];  // Airtable linked record returns array
      contact_id: string[];
      enrolled_at?: string;
      source: 'manual' | 'webhook';
    }

    export interface ScheduledMessage {
      id: string;
      campaign_id: string[];
      contact_id: string[];
      message_content: string;
      send_at: string; // UTC ISO8601
      offset_label: 'week_before' | 'day_before' | 'morning' | 'half_hour';
      status: 'pending' | 'sending' | 'sent' | 'failed';
      sent_at?: string;
    }

    export interface MessageLog {
      id: string;
      scheduled_message_id: string[];
      contact_id: string[];
      campaign_id: string[];
      status: 'sent' | 'failed';
      green_api_response?: string;
      error_message?: string;
      logged_at: string;
    }
    ```

    **`src/lib/airtable/campaigns.ts`**:
    ```typescript
    import { airtableBase } from './client';
    import type { Campaign } from './types';

    export async function getCampaigns(): Promise<Campaign[]> {
      const records = await airtableBase('Campaigns').select({
        sort: [{ field: 'event_date', direction: 'desc' }],
      }).all();
      return records.map((r) => ({ id: r.id, ...r.fields } as Campaign));
    }

    export async function getCampaignById(id: string): Promise<Campaign | null> {
      try {
        const record = await airtableBase('Campaigns').find(id);
        return { id: record.id, ...record.fields } as Campaign;
      } catch {
        return null;
      }
    }
    ```

    **`src/lib/airtable/contacts.ts`** — Same pattern as campaigns.ts but for Contacts table.

    **`src/lib/airtable/__tests__/campaigns.test.ts`** — Unit tests using jest with mocked Airtable:
    ```typescript
    // Mock airtableBase before import
    jest.mock('../client', () => ({
      airtableBase: jest.fn(),
    }));

    // Test: getCampaigns returns [] when Airtable returns empty
    // Test: getCampaignById returns null when record not found (catch returns null)
    // Test: getCampaigns maps record fields correctly to Campaign interface
    ```

    Configure jest in package.json (or jest.config.ts) to support TypeScript via ts-jest.
  </action>
  <verify>
    <automated>cd "d:/CLAUDE CODE HERE/MICAHL CRM" && npx tsc --noEmit && npm test -- --testPathPattern="airtable" 2>&1 | tail -20</automated>
  </verify>
  <done>
    TypeScript compiles with no errors. Unit tests pass. `src/lib/airtable/client.ts` contains `import 'server-only'`. Importing client.ts without env vars set throws the documented error. All 5 type interfaces are exported from types.ts.
  </done>
</task>

<task type="auto">
  <name>Task 2: Hebrew RTL layout, sidebar navigation, and Settings page</name>
  <files>src/app/layout.tsx, src/components/layout/Sidebar.tsx, src/components/layout/MobileHeader.tsx, src/app/page.tsx, src/app/kampanim/page.tsx, src/app/anshei-kesher/page.tsx, src/app/hagdarot/page.tsx</files>
  <action>
    Build the persistent Hebrew RTL dashboard shell and all 4 navigation destinations.

    **`src/components/layout/Sidebar.tsx`** — Persistent right-side sidebar:
    - Uses shadcn/ui components (Sheet for mobile overlay, nav links as Next.js `<Link>`)
    - RTL: sidebar is on the RIGHT side of the layout (CSS: `right-0` or `flex-row-reverse` parent)
    - Navigation items with Hebrew labels and simple icons (use lucide-react icons):
      - תפריט (LayoutDashboard icon) -> href="/"
      - קמפיינים (Megaphone icon) -> href="/kampanim"
      - אנשי קשר (Users icon) -> href="/anshei-kesher"
      - הגדרות (Settings icon) -> href="/hagdarot"
    - Active link highlighted with a distinct background
    - Desktop: always-visible sidebar, fixed width ~220px
    - Mobile: hidden, exposed via hamburger button in MobileHeader

    **`src/components/layout/MobileHeader.tsx`** — Mobile-only top bar:
    - Shows hamburger button on the RIGHT side (RTL)
    - App title "מיכל CRM" on the LEFT side (RTL layout means this appears on the right in visual order)
    - Tapping the hamburger opens the Sidebar as a Sheet overlay (shadcn Sheet component)
    - Hidden on `md:` and above (Tailwind responsive)

    **`src/app/layout.tsx`** — Update root layout:
    ```tsx
    // Keep: <html lang="he" dir="rtl">
    // Add: flex layout with Sidebar on right, main content on left
    // Structure:
    // <body className="flex flex-row-reverse min-h-screen">
    //   <Sidebar />
    //   <div className="flex-1 flex flex-col">
    //     <MobileHeader />
    //     <main className="flex-1 p-6">{children}</main>
    //   </div>
    // </body>
    ```

    **`src/app/page.tsx`** — Dashboard home (overview):
    Replace placeholder with a real overview layout:
    - Heading: "ברוך הבא, מיכל"
    - 3 stat cards: "קמפיינים פעילים", "אנשי קשר", "הודעות שנשלחו החודש"
    - Each card shows "--" as the value for now (no live data fetching yet -- Phase 2 wires the data)
    - GREEN API status indicator: colored dot + text "סטטוס GREEN API: לא מוגדר" (gray, Phase 4 makes it live)
    - Use shadcn/ui `Card` component

    **`src/app/kampanim/page.tsx`** — Campaigns placeholder:
    ```tsx
    <div className="text-center py-20">
      <h1 className="text-2xl font-bold mb-4">קמפיינים</h1>
      <p className="text-muted-foreground">בקרוב -- ניהול קמפיינים יהיה זמין בשלב 3</p>
    </div>
    ```

    **`src/app/anshei-kesher/page.tsx`** — Contacts placeholder (same pattern with "אנשי קשר" heading and "בקרוב -- ניהול אנשי קשר יהיה זמין בשלב 2").

    **`src/app/hagdarot/page.tsx`** — Settings page with integration guides (UX-03):
    This page satisfies UX-03: step-by-step instructions for GREEN API and MAKE.com, written in Hebrew, inside the dashboard.

    Structure using shadcn/ui Tabs or Accordion:

    Section 1: GREEN API
    Title: "חיבור GREEN API"
    Steps:
    1. כנסי לאתר green-api.com וצרי חשבון
    2. צרי Instance חדש -- בחרי בתוכנית המתאימה לנפח ההודעות שלך
    3. העתיקי את ה-Instance ID ואת ה-API Token מדף ה-Instance
    4. הכניסי את הערכים ב-Railway Environment Variables:
       - GREEN_API_INSTANCE_ID -- מזהה ה-Instance
       - GREEN_API_TOKEN -- הטוקן שלך
    5. חברי את חשבון הוואטסאפ שלך ל-Instance דרך ממשק GREEN API (סריקת QR)
    6. שמרי ואתחלי מחדש את השירות ב-Railway

    Section 2: MAKE.com Webhook
    Title: "הגדרת Webhook מ-MAKE.com"
    Steps:
    1. פתחי את ה-Scenario ב-MAKE.com שמקבל נרשמות מרב מסר
    2. הוסיפי מודול "HTTP > Make a request" בסוף ה-Scenario
    3. הגדירי:
       - URL: https://YOUR-RAILWAY-DOMAIN/api/webhook/contact (החליפי YOUR-RAILWAY-DOMAIN בכתובת Railway שלך)
       - Method: POST
       - Body type: JSON
       - Body: { "full_name": "שם", "phone": "טלפון" }
    4. שמרי והפעילי את ה-Scenario
    5. בדקי שנרשמת חדשה מופיעה ב-CRM תוך מספר שניות

    Display the webhook URL prominently: read NEXT_PUBLIC_APP_URL from env and display {NEXT_PUBLIC_APP_URL}/api/webhook/contact in a copyable code block.

    Add a note at the top of Settings: "הגדרות אלו נדרשות פעם אחת בלבד בהקמת המערכת"
  </action>
  <verify>
    <automated>cd "d:/CLAUDE CODE HERE/MICAHL CRM" && npm run build 2>&1 | tail -20</automated>
  </verify>
  <done>
    `npm run build` exits 0. All 4 routes render without errors (/, /kampanim, /anshei-kesher, /hagdarot). Sidebar appears on the right side of the layout. Settings page has step-by-step guides for both GREEN API and MAKE.com in Hebrew. Mobile hamburger button is present on narrow viewports. TypeScript compiles clean.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Verify Hebrew RTL layout and Settings page</name>
  <files>none -- human verification only</files>
  <action>Human verifies the running application visually and functionally per the how-to-verify steps below.</action>
  <what-built>
    Full Hebrew RTL dashboard layout with sidebar navigation + Settings page integration guides.
    Server-only Airtable service layer with type definitions.
    All 4 routes: / (overview), /kampanim (placeholder), /anshei-kesher (placeholder), /hagdarot (settings).
  </what-built>
  <how-to-verify>
    1. Run `npm run dev` and open http://localhost:3000
    2. Verify: sidebar appears on the RIGHT side, all text is right-aligned (Hebrew RTL)
    3. Click each sidebar item -- קמפיינים and אנשי קשר should show "בקרוב" messages
    4. Open הגדרות -- verify GREEN API and MAKE.com step-by-step guides appear in Hebrew
    5. Resize browser to mobile width -- verify hamburger button appears and sidebar opens as overlay when tapped
    6. Open browser DevTools > Network tab -- navigate to / -- confirm NO requests go to api.airtable.com from the client (all Airtable is server-side)
    7. Verify: page title is "מיכל CRM"
  </how-to-verify>
  <verify>
    <automated>HUMAN_VERIFY -- run `npm run dev` and follow the how-to-verify steps above</automated>
  </verify>
  <done>User types "approved" confirming RTL layout renders correctly, Settings page guides are present, and no Airtable token appears in browser Network tab.</done>
  <resume-signal>Type "approved" if the layout looks correct, or describe any visual/functional issues</resume-signal>
</task>

</tasks>

<verification>
- `npm run build` exits 0, zero TypeScript errors
- `src/lib/airtable/client.ts` contains `import 'server-only'`
- Unit tests pass for campaigns.ts and contacts.ts service functions
- `/hagdarot` route contains step-by-step guides for both GREEN API and MAKE.com (Hebrew text)
- `src/app/layout.tsx` has `dir="rtl"` and `lang="he"` and includes `<Sidebar />`
- No Airtable token or GREEN API credential appears in any client-side bundle (verify via build output)
- All 4 routes respond with 200 in production build
</verification>

<success_criteria>
Dashboard loads with Hebrew RTL layout. Sidebar on the right with 4 Hebrew navigation items. Campaigns and Contacts show "בקרוב" placeholders. Settings page has complete step-by-step guides for GREEN API and MAKE.com webhook setup -- Michal can configure both integrations without external help. Airtable service layer is server-only -- token never reaches the browser.
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation/01-03-SUMMARY.md` documenting:
- Exact route structure created (file paths -> URL paths)
- Service layer exports (function signatures for Phase 2 to extend)
- Any shadcn/ui components installed
- RTL implementation approach used
- Checkpoint outcome (approved / issues found and fixed)
- Files created
</output>
