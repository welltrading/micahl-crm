---
phase: 01-foundation
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - scripts/setup-airtable.ts
  - scripts/seed-airtable.ts
  - docs/airtable-schema.md
autonomous: true
requirements:
  - INFRA-01

must_haves:
  truths:
    - "Airtable base has exactly 5 tables: Campaigns, Contacts, CampaignEnrollments, ScheduledMessages, MessageLog"
    - "All tables have correct fields with Hebrew display names matching the schema definition"
    - "Sample seed data exists in the base so Michal can see how the system looks during development"
    - "The schema creation script is idempotent — running it twice does not create duplicate tables"
    - "Field names used in code (API names) are documented alongside Hebrew display names"
  artifacts:
    - path: "scripts/setup-airtable.ts"
      provides: "Airtable schema creation script — creates all 5 tables with fields via Airtable Meta API"
      exports: []
    - path: "scripts/seed-airtable.ts"
      provides: "Seed data script — inserts sample campaigns, contacts, enrollments, messages for dev"
    - path: "docs/airtable-schema.md"
      provides: "Human-readable schema reference: table names, field names (Hebrew display + API), types, relationships"
  key_links:
    - from: "scripts/setup-airtable.ts"
      to: "Airtable Meta API"
      via: "AIRTABLE_API_TOKEN + AIRTABLE_BASE_ID env vars"
      pattern: "AIRTABLE_API_TOKEN|AIRTABLE_BASE_ID"
    - from: "docs/airtable-schema.md"
      to: "src/lib/airtable/"
      via: "API field names referenced in service layer (Plan 03)"
      pattern: "fieldName|api_name"
---

<objective>
Define and create the complete Airtable schema for the CRM — all 5 tables with Hebrew field names — plus seed data for development.

Purpose: Airtable is the database. Every other phase reads/writes these tables. Getting the schema right now prevents costly migrations later.
Output: A running Airtable base with correct schema + seed data. A schema creation script. A reference document with all field names (Hebrew display + code API names).
</objective>

<execution_context>
@C:/Users/londi/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/londi/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/01-foundation/01-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create Airtable schema definition and setup script</name>
  <files>scripts/setup-airtable.ts, docs/airtable-schema.md</files>
  <action>
    Create the schema setup script and documentation. This plan runs PARALLEL to Plan 01 — scripts/ does not depend on the Next.js app.

    **Schema design — table names (English for Airtable API reliability) and Hebrew display names:**

    The CONTEXT.md says "Hebrew field names throughout" and "Claude's Discretion: Table names". Decision: Use English table names (API stability, Airtable API uses table names in URLs) but Hebrew field display names.

    **Table: Campaigns**
    | API Field Name | Hebrew Display | Type | Notes |
    |---|---|---|---|
    | campaign_name | שם קמפיין | Single line text | Required |
    | event_date | תאריך אירוע | Date (include time) | ISO8601 UTC |
    | description | תיאור | Long text | Optional |
    | status | סטטוס | Single select | options: future/active/ended — values: עתידי/פעיל/הסתיים |
    | created_at | נוצר בתאריך | Created time | Auto |

    **Table: Contacts**
    | API Field Name | Hebrew Display | Type | Notes |
    |---|---|---|---|
    | full_name | שם מלא | Single line text | Required |
    | phone | טלפון | Phone | Required, stored normalized: 972XXXXXXXXXX |
    | joined_date | תאריך הצטרפות | Date | |
    | notes | הערות | Long text | Optional |
    | created_at | נוצר בתאריך | Created time | Auto |

    **Table: CampaignEnrollments**
    | API Field Name | Hebrew Display | Type | Notes |
    |---|---|---|---|
    | campaign_id | קמפיין | Link to Campaigns | Required |
    | contact_id | איש קשר | Link to Contacts | Required |
    | enrolled_at | תאריך רישום | Date (include time) | |
    | source | מקור | Single select | options: manual/webhook — values: ידני/Webhook |

    **Table: ScheduledMessages**
    | API Field Name | Hebrew Display | Type | Notes |
    |---|---|---|---|
    | campaign_id | קמפיין | Link to Campaigns | Required |
    | contact_id | איש קשר | Link to Contacts | Required |
    | message_content | תוכן ההודעה | Long text | Required |
    | send_at | שליחה בשעה | Date (include time) | UTC, required |
    | offset_label | תזמון | Single select | options: week_before/day_before/morning/half_hour — values: שבוע לפני/יום לפני/בוקר האירוע/חצי שעה לפני |
    | status | סטטוס | Single select | options: pending/sending/sent/failed — values: ממתינה/בשליחה/נשלחה/נכשלה |
    | sent_at | נשלח בשעה | Date (include time) | Set on success |

    **Table: MessageLog**
    | API Field Name | Hebrew Display | Type | Notes |
    |---|---|---|---|
    | scheduled_message_id | הודעה מתוזמנת | Link to ScheduledMessages | Required |
    | contact_id | איש קשר | Link to Contacts | Required |
    | campaign_id | קמפיין | Link to Campaigns | Required |
    | status | סטטוס | Single select | options: sent/failed — values: נשלחה/נכשלה |
    | green_api_response | תגובת GREEN API | Long text | Raw JSON response for debugging |
    | error_message | הודעת שגיאה | Long text | Human-readable Hebrew error |
    | logged_at | תאריך רישום | Created time | Auto |

    **Create `scripts/setup-airtable.ts`:**

    Use the Airtable Meta API (https://airtable.com/developers/web/api/create-table) to create tables programmatically. The script reads AIRTABLE_API_TOKEN and AIRTABLE_BASE_ID from environment.

    Structure:
    ```typescript
    import * as dotenv from 'dotenv';
    dotenv.config({ path: '.env.local' });

    const BASE_URL = `https://api.airtable.com/v0/meta/bases/${process.env.AIRTABLE_BASE_ID}/tables`;
    const headers = {
      Authorization: `Bearer ${process.env.AIRTABLE_API_TOKEN}`,
      'Content-Type': 'application/json',
    };

    // Define each table as a const with fields array
    // Use fetch() (Node 18+) to POST to the Meta API
    // Check if table already exists before creating (idempotency)
    // Log: "Creating table: X" / "Table already exists: X — skipping"
    ```

    For each table: POST to the Meta API with `{ name: "TableName", fields: [...] }`.

    Idempotency: First GET `/v0/meta/bases/{baseId}/tables` to list existing tables. Skip any that already exist by name.

    **Create `docs/airtable-schema.md`:**

    Full human-readable schema reference. Include:
    - All 5 tables with field API names, Hebrew display names, types, and notes
    - Relationships diagram (text-based, e.g., CampaignEnrollments links Campaigns ↔ Contacts)
    - Note: "Table names are English (API stability). Field display names are Hebrew (Michal's UX)."
    - Note: "AIRTABLE_BASE_ID is the base used for all operations. Never hardcode."
  </action>
  <verify>
    <automated>cd "d:/CLAUDE CODE HERE/MICAHL CRM" && npx tsc --noEmit scripts/setup-airtable.ts 2>&1 | head -30</automated>
  </verify>
  <done>
    `scripts/setup-airtable.ts` exists and parses without TypeScript errors. `docs/airtable-schema.md` contains all 5 tables with field definitions. The script has idempotency logic (checks existing tables before creating). When run with real credentials, it creates all 5 tables in the Airtable base.
  </done>
</task>

<task type="auto">
  <name>Task 2: Create seed data script</name>
  <files>scripts/seed-airtable.ts</files>
  <action>
    Create `scripts/seed-airtable.ts` that inserts representative sample data for development. Michal will delete this data when going live (noted in the script header comment).

    **Seed data to insert:**

    Campaigns (2 records):
    - קמפיין סדנת יוגה — event_date: 2 weeks from today, status: עתידי
    - קמפיין מדיטציה חורף — event_date: 3 months ago, status: הסתיים

    Contacts (3 records):
    - רחל כהן — phone: 972501234567 — joined 1 month ago
    - מרים לוי — phone: 972521234568 — joined 2 months ago
    - דינה אברהם — phone: 972541234569 — joined 1 week ago

    CampaignEnrollments (3 records — all contacts enrolled in the yoga campaign):
    - Each contact linked to the yoga campaign, source: ידני

    ScheduledMessages (2 records for yoga campaign, contact: רחל כהן):
    - offset_label: שבוע לפני, status: נשלחה, message_content: "שלום {{שם}}, תזכורת לסדנה בעוד שבוע!"
    - offset_label: יום לפני, status: ממתינה, message_content: "סדנת היוגה מחר — מחכים לך!"

    MessageLog (1 record — the sent message above):
    - status: נשלחה, green_api_response: '{"status":"ok"}', logged_at: auto

    Script structure:
    ```typescript
    import * as dotenv from 'dotenv';
    dotenv.config({ path: '.env.local' });
    import Airtable from 'airtable';

    // Use Airtable SDK (not Meta API) for record creation
    const base = new Airtable({ apiKey: process.env.AIRTABLE_API_TOKEN })
      .base(process.env.AIRTABLE_BASE_ID!);

    // Insert in dependency order: Campaigns → Contacts → Enrollments → Messages → Log
    // Log each insertion: "Seeded: [table] — [record name]"
    ```

    Add both scripts to package.json:
    ```json
    "setup:airtable": "npx ts-node scripts/setup-airtable.ts",
    "seed:airtable": "npx ts-node scripts/seed-airtable.ts"
    ```
  </action>
  <verify>
    <automated>cd "d:/CLAUDE CODE HERE/MICAHL CRM" && npx tsc --noEmit scripts/seed-airtable.ts 2>&1 || npx ts-node --transpile-only scripts/seed-airtable.ts --check 2>&1 | head -10</automated>
  </verify>
  <done>
    `scripts/seed-airtable.ts` exists. TypeScript compiles without errors. `package.json` has `"setup:airtable"` and `"seed:airtable"` scripts. When run against a base that has been set up with setup-airtable.ts, it inserts all seed records successfully.
  </done>
</task>

</tasks>

<verification>
- `scripts/setup-airtable.ts` exists with all 5 table definitions
- `scripts/seed-airtable.ts` exists with sample data for all 5 tables
- `docs/airtable-schema.md` documents all tables with Hebrew display names and API field names
- Both scripts have TypeScript types (no `any` for field names)
- Idempotency: setup script checks existing tables before creating
- `package.json` has `setup:airtable` and `seed:airtable` scripts
- No API token is hardcoded — scripts read from `.env.local`
</verification>

<success_criteria>
Running `npm run setup:airtable` against a real Airtable base creates all 5 tables with correct Hebrew field names. Running `npm run seed:airtable` inserts sample data. The schema reference doc is accurate and complete.
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation/01-02-SUMMARY.md` documenting:
- Exact field API names chosen for each table (the service layer in Plan 03 will use these)
- Any fields added or changed from the plan
- Confirmation that seed data was verified against a real Airtable base (or note if deferred to manual testing)
- Files created
</output>
