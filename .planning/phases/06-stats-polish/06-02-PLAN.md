---
phase: 06-stats-polish
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/app/layout.tsx
  - src/components/contacts/ContactDetailPanel.tsx
  - src/components/contacts/ContactsPageClient.tsx
autonomous: false
requirements:
  - CONT-03
must_haves:
  truths:
    - "ContactDetailPanel מוצגת בצורה קריאה במובייל — לא חתוך, לא גלילה אופקית"
    - "דף אנשי קשר ודף קמפיינים קריאים במובייל (375px) ללא גלילה אופקית"
    - "ניווט (MobileHeader + Sidebar) לא נפגע"
  artifacts:
    - path: "src/app/layout.tsx"
      provides: "Responsive main padding — p-4 md:p-6 (reduce mobile double-padding)"
      contains: "p-4 md:p-6"
    - path: "src/components/contacts/ContactDetailPanel.tsx"
      provides: "Explicit mobile width on SheetContent"
      contains: "w-full sm:max-w-md"
  key_links:
    - from: "src/app/layout.tsx"
      to: "main padding"
      via: "Tailwind responsive classes"
      pattern: "p-4 md:p-6"
    - from: "src/components/contacts/ContactDetailPanel.tsx"
      to: "SheetContent"
      via: "className prop"
      pattern: "w-full sm:max-w-md"
---

<objective>
Fix mobile responsive issues across the dashboard: reduce double-padding in layout, add explicit SheetContent width on ContactDetailPanel so the panel is readable on small screens, and remove page-level padding duplication in ContactsPageClient.

Purpose: Satisfies CONT-03 mobile scope — campaigns list and contacts list readable on mobile with no horizontal scroll or clipped text.
Output: Targeted CSS fixes in 3 files, verified by human visual checkpoint on mobile viewport.
</objective>

<execution_context>
@C:/Users/londi/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/londi/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/06-stats-polish/06-CONTEXT.md
@.planning/phases/06-stats-polish/06-RESEARCH.md

<interfaces>
<!-- Current state of files being modified. No exploration needed. -->

From src/app/layout.tsx (line 24):
```tsx
<main className="flex-1 p-6">{children}</main>
```
Problem: layout has p-6 AND ContactsPageClient outer div has p-6 → double 24px padding on mobile.
Fix: Change layout.tsx main to `p-4 md:p-6` to reduce mobile padding.

From src/components/contacts/ContactDetailPanel.tsx (line 66):
```tsx
<SheetContent side="left">
```
Problem: No explicit width — defaults to shadcn's fixed rem value, may overflow or be too narrow on mobile.
Fix: Add className="w-full sm:max-w-md overflow-y-auto"

From src/components/contacts/ContactsPageClient.tsx (line 50):
```tsx
<div className="flex flex-col gap-6 p-6">
```
Note: layout.tsx already pads with p-4 md:p-6 after our fix. The page-level p-6 creates additional padding.
Fix: Remove p-6 from outer div → `flex flex-col gap-6` (layout main handles padding).

Files to NOT change (already mobile-safe per research):
- src/components/layout/MobileHeader.tsx — already correct
- src/components/layout/Sidebar.tsx — already `hidden md:flex`
- src/components/campaigns/CampaignSheet.tsx — already `w-full sm:max-w-lg overflow-y-auto`
- src/components/contacts/ContactsTable.tsx — already has `overflow-x-auto`
- src/components/contacts/AddContactModal.tsx — already `w-full max-w-md`
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix mobile layout padding and ContactDetailPanel width</name>
  <files>src/app/layout.tsx, src/components/contacts/ContactDetailPanel.tsx, src/components/contacts/ContactsPageClient.tsx</files>
  <action>
    CHANGE 1 — src/app/layout.tsx line 24:
    Change:  `<main className="flex-1 p-6">`
    To:      `<main className="flex-1 p-4 md:p-6">`
    Reason: Reduces mobile padding from 24px to 16px, eliminating tight double-padding when combined with page-level padding.

    CHANGE 2 — src/components/contacts/ContactDetailPanel.tsx line 66:
    Change:  `<SheetContent side="left">`
    To:      `<SheetContent side="left" className="w-full sm:max-w-md overflow-y-auto">`
    Reason: CampaignSheet already uses `w-full sm:max-w-lg` — apply same pattern for ContactDetailPanel. Prevents overflow on mobile viewport.

    CHANGE 3 — src/components/contacts/ContactsPageClient.tsx outer div (line 50):
    Change:  `<div className="flex flex-col gap-6 p-6">`
    To:      `<div className="flex flex-col gap-6">`
    Reason: layout.tsx main now pads with p-4 md:p-6. Removing page-level p-6 eliminates the double-padding (48px combined) that causes content to feel cramped on mobile.

    Do NOT modify MobileHeader, Sidebar, CampaignSheet, ContactsTable, AddContactModal — confirmed mobile-safe by research.
    Do NOT add any new components or animation.
    Do NOT change Campaigns page padding in this task — investigate only if visual check fails (Claude's Discretion).
  </action>
  <verify>
    <automated>npx tsc --noEmit</automated>
  </verify>
  <done>
    TypeScript compiles cleanly. Three targeted changes made: layout main padding reduced, ContactDetailPanel SheetContent has explicit width, ContactsPageClient outer div has no page-level padding.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Human verify mobile responsive on 375px viewport</name>
  <files>none</files>
  <action>Human verifies the visual result of Task 1 on a mobile viewport. No code changes in this task.</action>
  <verify>
    <automated>npx jest --passWithNoTests</automated>
  </verify>
  <done>User confirms all mobile checks pass (or reports issues for follow-up fix).</done>
  <what-built>
    Mobile responsive fixes:
    - layout.tsx: main padding reduced to p-4 md:p-6 (was p-6)
    - ContactDetailPanel: SheetContent now has w-full sm:max-w-md overflow-y-auto
    - ContactsPageClient: outer div padding removed (layout handles it)
  </what-built>
  <how-to-verify>
    1. Open Chrome DevTools, click the mobile device icon (or press Ctrl+Shift+M)
    2. Set viewport to 375px width (iPhone SE size)
    3. Visit http://localhost:3001/anshei-kesher
       - Verify: contact list and stat cards visible, no horizontal scrollbar
       - Verify: page content not cramped against screen edges, readable padding
       - Verify: growth table (from Plan 01) fits without horizontal overflow
    4. Click any contact to open ContactDetailPanel
       - Verify: panel fills most of the screen width, text readable, not clipped
       - Verify: can scroll vertically through message history if long
    5. Visit http://localhost:3001/kampaynot
       - Verify: campaign cards readable, no horizontal scrollbar
    6. Confirm MobileHeader (hamburger) still appears and opens sidebar nav
  </how-to-verify>
  <resume-signal>Type "approved" if everything looks correct, or describe specific issues (e.g., "panel too narrow", "campaigns page has horizontal scroll")</resume-signal>
</task>

</tasks>

<verification>
1. `npx tsc --noEmit` — clean after CSS class changes
2. `npx jest` — full suite green (no regressions from layout changes)
3. Human visual: 375px viewport, contacts + campaigns pages, ContactDetailPanel — all pass checklist above
</verification>

<success_criteria>
- ContactDetailPanel opens and displays correctly at 375px viewport width
- Contacts page: no horizontal scrollbar, content readable, adequate padding
- Campaigns page: no horizontal scrollbar, campaign cards readable
- MobileHeader navigation still functional
- No TypeScript errors introduced
- Full test suite remains green
</success_criteria>

<output>
After completion, create `.planning/phases/06-stats-polish/06-02-SUMMARY.md` with:
- What was fixed (layout padding, ContactDetailPanel width, ContactsPageClient padding)
- User approval notes from checkpoint
- Files modified
- Any issues found and fixed during visual review
</output>
