---
phase: 06-stats-polish
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/lib/airtable/__tests__/contacts.test.ts
  - src/components/contacts/ContactsPageClient.tsx
autonomous: true
requirements:
  - CONT-03
must_haves:
  truths:
    - "מיכל רואה טבלת 'צמיחת קהל לפי חודש' מתחת לכרטיסי הסטטיסטיקות הקיימים"
    - "הטבלה מציגה רק חודשים שיש בהם הצטרפויות (אין שורות עם 0)"
    - "הטבלה מסודרת מהחדש למישן (descending)"
    - "ברירת מחדל: 3 חודשים אחרונים — שני שדות From/To מוצגים ומאפשרים סינון"
    - "כשאין הצטרפויות בטווח: מוצגת הודעה 'אין הצטרפויות בטווח התאריכים שנבחר'"
  artifacts:
    - path: "src/lib/airtable/__tests__/contacts.test.ts"
      provides: "Unit tests for aggregateByMonth pure function"
      contains: "describe('aggregateByMonth'"
    - path: "src/components/contacts/ContactsPageClient.tsx"
      provides: "Growth table UI with date range filter"
      contains: "aggregateByMonth"
  key_links:
    - from: "src/components/contacts/ContactsPageClient.tsx"
      to: "aggregateByMonth (inline pure function)"
      via: "React.useMemo"
      pattern: "useMemo.*aggregateByMonth|aggregateByMonth.*useMemo"
---

<objective>
Add a month-by-month growth table to the contacts page showing how many contacts joined each month, with a date range filter (From/To date pickers). All computation is client-side from the existing `contacts` prop — zero new Airtable calls.

Purpose: Satisfies CONT-03 — Michal can see growth trends over time without opening Airtable.
Output: Growth table rendered below existing stat cards in ContactsPageClient, with unit tests for the aggregation logic.
</objective>

<execution_context>
@C:/Users/londi/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/londi/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-stats-polish/06-CONTEXT.md
@.planning/phases/06-stats-polish/06-RESEARCH.md

<interfaces>
<!-- Key types the executor needs. No codebase exploration needed. -->

From src/lib/airtable/types.ts:
```typescript
export interface Contact {
  id: string;
  full_name: string;
  phone: string; // normalized: 972XXXXXXXXXX
  joined_date?: string;
  notes?: string;
  created_at: string; // ISO8601 — use this for growth aggregation
}
```

From src/components/contacts/ContactsPageClient.tsx — current structure:
```typescript
// Props
interface ContactsPageClientProps {
  contacts: Contact[];
}

// Existing stat cards (growth table goes AFTER this block, BEFORE search input)
<div className="grid grid-cols-1 gap-4 sm:grid-cols-3">
  <Card>סך אנשי קשר...</Card>
  <Card>הצטרפו החודש...</Card>
  <Card>הצטרפו השבוע...</Card>
</div>

// Search input (growth table is inserted before this)
<div>
  <input type="search" ... />
</div>

// Contacts table
<ContactsTable contacts={filtered} onContactClick={setSelectedContact} />
```

From src/components/ui/card.tsx:
```typescript
// Already installed — used for stat cards; use same for growth table
import { Card, CardContent, CardHeader, CardTitle } from '@/components/ui/card';
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add aggregateByMonth unit tests and implement pure function in ContactsPageClient</name>
  <files>src/lib/airtable/__tests__/contacts.test.ts, src/components/contacts/ContactsPageClient.tsx</files>
  <behavior>
    - Test 1: contacts grouped by month — 3 contacts in Jan 2026, 2 in Feb 2026 → [{key:'2026-02', count:2}, {key:'2026-01', count:3}] (newest first)
    - Test 2: date range filter — contacts outside from/to window excluded
    - Test 3: months with zero contacts not included in output — no zero-count rows
    - Test 4: empty contacts array → returns []
    - Test 5: from/to both null/empty → all contacts included (no filter)
    - Test 6: single contact → [{key:'YYYY-MM', count:1}]
    - Test 7: contacts spanning 4 months, filter to 2 months → only those 2 months returned
  </behavior>
  <action>
    STEP 1 — Add tests to src/lib/airtable/__tests__/contacts.test.ts:

    Append a new `describe('aggregateByMonth')` block at the end of the file. Import the function from `@/components/contacts/ContactsPageClient` once it's extracted (or test the logic inline using a helper).

    Since `aggregateByMonth` will live as an exported pure function in `ContactsPageClient.tsx`, the test imports it:
    ```typescript
    import { aggregateByMonth } from '@/components/contacts/ContactsPageClient';
    ```

    Write the 7 test cases above using Jest. Run: `npx jest --testPathPattern="contacts"` — expect RED (function not yet exported).

    STEP 2 — Implement aggregateByMonth in ContactsPageClient.tsx:

    Add an exported pure function (exported so tests can import it, used internally via useMemo):

    ```typescript
    export function aggregateByMonth(
      contacts: Contact[],
      fromStr: string,
      toStr: string
    ): { key: string; count: number }[] {
      const fromDate = fromStr ? new Date(fromStr + 'T00:00:00') : null;
      const toDate = toStr ? new Date(toStr + 'T23:59:59') : null;

      const counts: Record<string, number> = {};
      for (const c of contacts) {
        const d = new Date(c.created_at);
        if (fromDate && d < fromDate) continue;
        if (toDate && d > toDate) continue;
        const key = `${d.getFullYear()}-${String(d.getMonth() + 1).padStart(2, '0')}`;
        counts[key] = (counts[key] ?? 0) + 1;
      }

      return Object.entries(counts)
        .sort(([a], [b]) => b.localeCompare(a))
        .map(([key, count]) => ({ key, count }));
    }
    ```

    Run: `npx jest --testPathPattern="contacts"` — expect GREEN.

    STEP 3 — Add growth table UI to ContactsPageClient.tsx:

    Add state for date range:
    ```typescript
    function getDefaultFrom(): string {
      const d = new Date();
      d.setMonth(d.getMonth() - 3);
      return d.toISOString().slice(0, 10);
    }
    function getDefaultTo(): string {
      return new Date().toISOString().slice(0, 10);
    }

    const [fromStr, setFromStr] = React.useState(getDefaultFrom);
    const [toStr, setToStr] = React.useState(getDefaultTo);
    ```

    Add a helper for Hebrew month labels (Claude's Discretion — use Intl.DateTimeFormat):
    ```typescript
    function formatMonthLabel(key: string): string {
      const [year, month] = key.split('-').map(Number);
      return new Date(year, month - 1, 1).toLocaleDateString('he-IL', { month: 'long', year: 'numeric' });
    }
    ```

    Add useMemo for aggregation:
    ```typescript
    const growthData = React.useMemo(
      () => aggregateByMonth(contacts, fromStr, toStr),
      [contacts, fromStr, toStr]
    );
    ```

    Insert the growth table Card AFTER the stat cards grid and BEFORE the search input:
    ```tsx
    <Card>
      <CardHeader>
        <CardTitle className="text-sm font-semibold">צמיחת קהל לפי חודש</CardTitle>
        <div className="flex gap-2 items-center flex-wrap mt-2">
          <label className="text-xs text-muted-foreground">מ-</label>
          <input
            type="date"
            value={fromStr}
            dir="ltr"
            onChange={(e) => setFromStr(e.target.value)}
            className="rounded-md border px-2 py-1 text-sm bg-background focus:outline-none focus:ring-2 focus:ring-ring"
          />
          <label className="text-xs text-muted-foreground">עד</label>
          <input
            type="date"
            value={toStr}
            dir="ltr"
            onChange={(e) => setToStr(e.target.value)}
            className="rounded-md border px-2 py-1 text-sm bg-background focus:outline-none focus:ring-2 focus:ring-ring"
          />
        </div>
      </CardHeader>
      <CardContent>
        {growthData.length === 0 ? (
          <p className="text-sm text-muted-foreground">אין הצטרפויות בטווח התאריכים שנבחר</p>
        ) : (
          <table className="w-full text-sm">
            <thead>
              <tr className="border-b text-muted-foreground">
                <th className="pb-2 text-start font-medium">חודש</th>
                <th className="pb-2 text-start font-medium">הצטרפו</th>
              </tr>
            </thead>
            <tbody>
              {growthData.map(({ key, count }) => (
                <tr key={key} className="border-b last:border-0">
                  <td className="py-2 pe-4">{formatMonthLabel(key)}</td>
                  <td className="py-2 font-medium">{count}</td>
                </tr>
              ))}
            </tbody>
          </table>
        )}
      </CardContent>
    </Card>
    ```

    Do NOT add cumulative total column (deferred per CONTEXT.md).
    Do NOT add dir="ltr" to month name cells — Hebrew text must remain RTL.
    Do NOT use any chart library — plain table only (locked decision).
  </action>
  <verify>
    <automated>npx jest --testPathPattern="contacts" --passWithNoTests</automated>
  </verify>
  <done>
    All 7 aggregateByMonth unit tests pass. Growth table renders in ContactsPageClient below stat cards with From/To date pickers defaulting to last 3 months. TypeScript compiles without errors: `npx tsc --noEmit`.
  </done>
</task>

</tasks>

<verification>
1. `npx jest --testPathPattern="contacts"` — all tests green (including new aggregateByMonth suite)
2. `npx jest` — full test suite green
3. `npx tsc --noEmit` — no TypeScript errors
4. Dev server: visit http://localhost:3001/anshei-kesher — growth table visible below stat cards, date pickers functional, Hebrew month names displayed (ינואר 2026 format)
</verification>

<success_criteria>
- aggregateByMonth pure function passes all 7 unit tests
- Growth table renders with correct month grouping, descending sort, no zero-count rows
- Date range filter updates table reactively on change
- Default range is last 3 months (from = today - 3 months, to = today)
- Empty state message shown when no contacts in range
- No new Airtable API calls introduced
</success_criteria>

<output>
After completion, create `.planning/phases/06-stats-polish/06-01-SUMMARY.md` with:
- What was built (growth table + aggregateByMonth function)
- Key implementation decisions made
- Files modified
- Test results
</output>
