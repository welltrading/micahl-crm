---
phase: 03-campaign-management
plan: 01
type: tdd
wave: 1
depends_on: []
files_modified:
  - src/lib/airtable/__tests__/timezone.test.ts
  - src/lib/airtable/__tests__/scheduled-messages.test.ts
  - src/lib/airtable/__tests__/campaigns.test.ts
  - src/lib/airtable/timezone.ts
autonomous: true
requirements:
  - CAMP-03
must_haves:
  truths:
    - "computeSendAt('week_before') produces a UTC time exactly 7 days before the event at 09:00 Israel time"
    - "computeSendAt('day_before') produces a UTC time exactly 1 day before the event at 09:00 Israel time"
    - "computeSendAt('morning') uses the user-chosen hour in Israel time converted to UTC"
    - "computeSendAt('half_hour') computes event_time minus 30 minutes in Israel time converted to UTC"
    - "DST boundary event (April 1 2026): week_before lands March 25 at UTC+2, not UTC+3"
    - "All 4 offset labels produce different UTC values for the same event"
  artifacts:
    - path: "src/lib/airtable/timezone.ts"
      provides: "localIsraelToUTC and computeSendAt exported functions"
      exports: ["localIsraelToUTC", "computeSendAt"]
    - path: "src/lib/airtable/__tests__/timezone.test.ts"
      provides: "DST-safe UTC conversion tests including 2026 boundaries"
      contains: "Asia/Jerusalem"
    - path: "src/lib/airtable/__tests__/scheduled-messages.test.ts"
      provides: "Test stubs for upsertScheduledMessages and updateScheduledMessage"
    - path: "src/lib/airtable/__tests__/campaigns.test.ts"
      provides: "Extended tests for createCampaign, getEnrollmentCountsByCampaign, deriveCampaignStatus"
  key_links:
    - from: "src/lib/airtable/timezone.ts"
      to: "src/lib/airtable/scheduled-messages.ts"
      via: "computeSendAt imported and called in upsertScheduledMessages"
      pattern: "computeSendAt"
    - from: "src/lib/airtable/timezone.ts"
      to: "src/app/kampanim/actions.ts"
      via: "computeSendAt called when saving message slots"
      pattern: "computeSendAt"
---

<objective>
Build and TDD-verify the DST-safe timezone utility that all send_at calculations depend on, plus create test scaffolds for the Airtable service layer functions coming in Plan 02.

Purpose: The timezone utility is the most critical correctness requirement in Phase 3. An event on April 1 2026 spans the DST boundary — "week before" must land March 25 at UTC+2, not UTC+3. Getting this wrong silently corrupts every message timing in the system. TDD ensures the logic is correct before it is used.

Output: timezone.ts with localIsraelToUTC + computeSendAt, plus test scaffolds for scheduled-messages.ts and campaigns.ts extensions (Wave 0 for Plan 02).
</objective>

<execution_context>
@C:/Users/londi/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/londi/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-campaign-management/03-CONTEXT.md
@.planning/phases/03-campaign-management/03-RESEARCH.md

<interfaces>
<!-- Existing types from src/lib/airtable/types.ts -->
```typescript
export interface ScheduledMessage {
  id: string;
  campaign_id: string[];
  contact_id: string[];
  message_content: string;
  send_at: string; // UTC ISO8601
  offset_label: 'week_before' | 'day_before' | 'morning' | 'half_hour';
  status: 'pending' | 'sending' | 'sent' | 'failed';
  sent_at?: string;
}

export interface Campaign {
  id: string;
  campaign_name: string;
  event_date: string; // ISO8601 UTC
  description?: string;
  status: 'future' | 'active' | 'ended';
  created_at: string;
}
```

<!-- Existing test mock pattern from campaigns.test.ts -->
```typescript
const mockAll = jest.fn();
const mockFind = jest.fn();
const mockSelect = jest.fn(() => ({ all: mockAll }));
const mockTable = jest.fn(() => ({ select: mockSelect, find: mockFind }));

jest.mock('../client', () => ({
  airtableBase: mockTable,
}));
```

<!-- Israel DST 2026 facts -->
// Israel Standard Time: UTC+2 (winter, through March 26)
// Israel Daylight Time: UTC+3 (summer, March 27 onward)
// DST start 2026: March 27 at 02:00 local (clocks forward to 03:00)
// DST end 2026: October 25 at 02:00 local (clocks back to 01:00)
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Write failing tests for timezone.ts and extend test scaffolds</name>
  <files>
    src/lib/airtable/__tests__/timezone.test.ts,
    src/lib/airtable/__tests__/scheduled-messages.test.ts,
    src/lib/airtable/__tests__/campaigns.test.ts
  </files>
  <behavior>
    - timezone.test.ts — localIsraelToUTC:
      - "2026-04-14" + "09:00" → UTC is "2026-04-14T06:00:00.000Z" (IDT = UTC+3)
      - "2026-01-15" + "09:00" → UTC is "2026-01-15T07:00:00.000Z" (IST = UTC+2)
      - "2026-03-27" + "03:30" → UTC correct for post-transition IDT (UTC+3 on that day after 02:00)
    - timezone.test.ts — computeSendAt:
      - event "2026-04-14" "19:00", offset "week_before" → send_at lands March 25 at 09:00 IL = 07:00 UTC → "2026-03-25T07:00:00.000Z"
      - event "2026-04-14" "19:00", offset "day_before" → April 13 at 09:00 IL = 06:00 UTC → "2026-04-13T06:00:00.000Z"
      - event "2026-04-14" "19:00", offset "morning", userHour "08:00" → April 14 at 08:00 IL = 05:00 UTC → "2026-04-14T05:00:00.000Z"
      - event "2026-04-14" "19:00", offset "half_hour" → April 14 at 18:30 IL = 15:30 UTC → "2026-04-14T15:30:00.000Z"
      - DST boundary: event "2026-04-01" "09:00", offset "week_before" → March 25 at 09:00 IL = March 25 at 07:00 UTC (UTC+2, before DST) → "2026-03-25T07:00:00.000Z"
    - scheduled-messages.test.ts — stubs (all should fail RED):
      - upsertScheduledMessages: "creates Airtable records for filled slots only"
      - upsertScheduledMessages: "skips slots with empty message_content"
      - upsertScheduledMessages: "wraps campaignId in array for linked record field"
      - updateScheduledMessage: "patches תוכן ההודעה when message_content provided"
      - updateScheduledMessage: "patches שליחה בשעה when send_at provided"
    - campaigns.test.ts extensions (add to end of existing file — do NOT delete existing tests):
      - createCampaign: "calls Airtable create with correct Hebrew field names"
      - createCampaign: "returns created Campaign object"
      - getEnrollmentCountsByCampaign: "returns empty object when no enrollments"
      - getEnrollmentCountsByCampaign: "counts correctly when multiple enrollments reference same campaign"
      - deriveCampaignStatus: "returns 'future' when event_date is in the future"
      - deriveCampaignStatus: "returns 'active' when event_date is today"
      - deriveCampaignStatus: "returns 'ended' when event_date is in the past"
  </behavior>
  <action>
    Create src/lib/airtable/__tests__/timezone.test.ts — no mocking needed, timezone.ts uses only native Intl API. Import localIsraelToUTC and computeSendAt from '../timezone'. All tests will fail RED (file doesn't exist yet).

    Create src/lib/airtable/__tests__/scheduled-messages.test.ts — use the same mock pattern as campaigns.test.ts. Add mockCreate and mockUpdate functions. Mock '../client'. Import upsertScheduledMessages and updateScheduledMessage from '../scheduled-messages'. All tests will fail RED.

    Extend src/lib/airtable/__tests__/campaigns.test.ts — add new describe blocks at the END of the file (after existing getCampaignById tests). Add mockCreate to the mock setup at top. Import createCampaign, getEnrollmentCountsByCampaign, deriveCampaignStatus (these don't exist yet — tests will fail RED). Keep all existing tests intact.

    Run tests — confirm they fail (RED).
  </action>
  <verify>
    <automated>cd "D:\CLAUDE CODE HERE\MICAHL CRM" && npm test -- --testPathPattern="campaigns.test|scheduled-messages.test|timezone.test" 2>&1 | tail -20</automated>
  </verify>
  <done>Tests exist and fail with "Cannot find module" or "is not a function" — RED confirmed. Existing campaigns.test.ts tests still pass.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement timezone.ts to make timezone tests GREEN</name>
  <files>src/lib/airtable/timezone.ts</files>
  <behavior>
    - localIsraelToUTC("2026-04-14", "09:00") returns "2026-04-14T06:00:00.000Z"
    - localIsraelToUTC("2026-01-15", "09:00") returns "2026-01-15T07:00:00.000Z"
    - computeSendAt("2026-04-14", "19:00", "week_before") returns "2026-03-25T07:00:00.000Z"
    - computeSendAt("2026-04-14", "19:00", "half_hour") returns "2026-04-14T15:30:00.000Z"
    - computeSendAt("2026-04-01", "09:00", "week_before") returns "2026-03-25T07:00:00.000Z" (DST-safe)
  </behavior>
  <action>
    Create src/lib/airtable/timezone.ts with two exported functions:

    1. localIsraelToUTC(dateISO: string, timeHHMM: string): string
       - Implement using the iterative Intl.DateTimeFormat approach from RESEARCH.md Pattern 4
       - Step 1: Estimate UTC assuming UTC+2 (standard offset)
       - Step 2: Format that UTC moment back to Jerusalem time using Intl with timeZone: 'Asia/Jerusalem'
       - Step 3: Compare displayed Jerusalem hour to desired hour; if diff != 0 adjust UTC by the diff
       - Return utc.toISOString()

    2. computeSendAt(eventDateISO: string, eventTimeHHMM: string, offset: ScheduledMessage['offset_label'], userChosenHour?: string): string
       - Implement the OFFSET_CONFIGS map from RESEARCH.md Pattern 5:
         week_before: daysOffset=-7, fixedHour=9
         day_before: daysOffset=-1, fixedHour=9
         morning: daysOffset=0, fixedHour=null (use userChosenHour)
         half_hour: daysOffset=0, fixedHour=null (compute event_time - 30 min)
       - Adjust date by daysOffset using UTC date arithmetic (Date.UTC, setUTCDate)
       - Determine localTime in Jerusalem space FIRST, then call localIsraelToUTC
       - For half_hour: subtract 30 min from eventTimeHHMM arithmetically (e.g., 19:00 → 18:30, handle hour underflow)
       - Return localIsraelToUTC(adjustedDateISO, localTime)

    Import ScheduledMessage type from './types'.
    Add 'server-only' import at top (all airtable files are server-only).

    Run tests — confirm timezone.test.ts goes GREEN. scheduled-messages.test.ts and campaigns.test.ts new tests still RED.
  </action>
  <verify>
    <automated>cd "D:\CLAUDE CODE HERE\MICAHL CRM" && npm test -- --testPathPattern="timezone.test" 2>&1 | tail -20</automated>
  </verify>
  <done>All timezone.test.ts tests pass GREEN. computeSendAt handles all 4 offset labels including DST boundary correctly.</done>
</task>

</tasks>

<verification>
Full test suite runs without errors after both tasks:
- npm test -- --testPathPattern="campaigns.test|scheduled-messages.test|timezone.test"
- timezone.test.ts: all GREEN
- campaigns.test.ts: existing tests GREEN, new tests RED (stubs for Plan 02)
- scheduled-messages.test.ts: all RED (stubs for Plan 02)
</verification>

<success_criteria>
- src/lib/airtable/timezone.ts exists and exports localIsraelToUTC and computeSendAt
- All 5 timezone tests pass including the DST boundary case (April 1 event, week_before landing March 25)
- Test scaffolds for scheduled-messages.ts and campaigns.ts extensions exist and fail RED (ready for Plan 02)
- No existing tests broken
</success_criteria>

<output>
After completion, create .planning/phases/03-campaign-management/03-01-SUMMARY.md
</output>
