---
phase: 03-campaign-management
plan: 02
type: tdd
wave: 2
depends_on:
  - 03-01
files_modified:
  - src/lib/airtable/campaigns.ts
  - src/lib/airtable/scheduled-messages.ts
autonomous: true
requirements:
  - CAMP-01
  - CAMP-02
  - CAMP-03
  - CAMP-04
  - CAMP-05
  - CAMP-06
must_haves:
  truths:
    - "createCampaign() writes שם קמפיין, תאריך אירוע, שעת האירוע, תיאור to Airtable and returns a Campaign object"
    - "getEnrollmentCountsByCampaign() returns a map of campaignId → count from CampaignEnrollments table"
    - "deriveCampaignStatus() returns 'future' / 'active' / 'ended' based on event_date vs now"
    - "upsertScheduledMessages() creates Airtable records for filled slots only, wrapping campaignId in array"
    - "updateScheduledMessage() patches the correct Hebrew field names in Airtable"
    - "getCampaigns() calls deriveCampaignStatus() so status is always computed fresh (not read from Airtable)"
  artifacts:
    - path: "src/lib/airtable/campaigns.ts"
      provides: "createCampaign, getEnrollmentCountsByCampaign, deriveCampaignStatus added"
      exports: ["getCampaigns", "getCampaignById", "createCampaign", "getEnrollmentCountsByCampaign", "deriveCampaignStatus"]
    - path: "src/lib/airtable/scheduled-messages.ts"
      provides: "getScheduledMessagesByCampaign, upsertScheduledMessages, updateScheduledMessage"
      exports: ["getScheduledMessagesByCampaign", "upsertScheduledMessages", "updateScheduledMessage"]
  key_links:
    - from: "src/lib/airtable/campaigns.ts"
      to: "src/lib/airtable/timezone.ts"
      via: "createCampaign imports computeSendAt (used indirectly via actions.ts)"
      pattern: "deriveCampaignStatus"
    - from: "src/lib/airtable/scheduled-messages.ts"
      to: "src/lib/airtable/timezone.ts"
      via: "upsertScheduledMessages receives pre-computed send_at from actions layer"
      pattern: "send_at.*UTC"
    - from: "src/lib/airtable/scheduled-messages.ts"
      to: "Airtable ScheduledMessages table"
      via: "airtableBase('ScheduledMessages').create / .update"
      pattern: "קמפיין.*campaignId"
---

<objective>
Implement the Airtable service layer for campaign management: extend campaigns.ts with createCampaign, getEnrollmentCountsByCampaign, and deriveCampaignStatus; create scheduled-messages.ts with getScheduledMessagesByCampaign, upsertScheduledMessages, and updateScheduledMessage. Turn the RED test stubs from Plan 01 GREEN.

Purpose: The UI layer (Plans 03–04) and Server Actions depend on these service functions. Getting the Airtable field names and linked-record formats correct here prevents silent data corruption.

Output: Two server-only Airtable service files with all functions tested.
</objective>

<execution_context>
@C:/Users/londi/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/londi/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/03-campaign-management/03-CONTEXT.md
@.planning/phases/03-campaign-management/03-RESEARCH.md
@.planning/phases/03-campaign-management/03-01-SUMMARY.md

<interfaces>
<!-- From src/lib/airtable/types.ts -->
```typescript
export interface Campaign {
  id: string;
  campaign_name: string;
  event_date: string; // ISO8601 UTC
  description?: string;
  status: 'future' | 'active' | 'ended';
  created_at: string;
}

export interface ScheduledMessage {
  id: string;
  campaign_id: string[];
  message_content: string;
  send_at: string; // UTC ISO8601
  offset_label: 'week_before' | 'day_before' | 'morning' | 'half_hour';
  status: 'pending' | 'sending' | 'sent' | 'failed';
  sent_at?: string;
}
```

<!-- From src/lib/airtable/timezone.ts (created in Plan 01) -->
```typescript
export function localIsraelToUTC(dateISO: string, timeHHMM: string): string;
export function computeSendAt(
  eventDateISO: string,
  eventTimeHHMM: string,
  offset: ScheduledMessage['offset_label'],
  userChosenHour?: string
): string;
```

<!-- Airtable Hebrew field names established in schema (Phase 1) -->
// Campaigns table: 'שם קמפיין', 'תאריך אירוע', 'שעת האירוע', 'תיאור', 'נוצר בתאריך'
// ScheduledMessages table: 'קמפיין' (linked), 'תוכן ההודעה', 'שליחה בשעה', 'תזמון', 'סטטוס'
// CampaignEnrollments table: 'קמפיין' (linked record array)

<!-- FIND+ARRAYJOIN pattern for linked record filterByFormula (established Phase 2) -->
// filterByFormula: `FIND("${contactId}", ARRAYJOIN({קמפיין}))`
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Extend campaigns.ts with createCampaign, getEnrollmentCountsByCampaign, deriveCampaignStatus</name>
  <files>src/lib/airtable/campaigns.ts</files>
  <behavior>
    - createCampaign({ campaign_name, event_date, event_time, description? }):
      - Calls airtableBase('Campaigns').create() with correct Hebrew fields
      - Uses typecast: true for date field (established Phase 2 pattern)
      - Returns Campaign object mapped from the created record
      - Status on returned object is derived via deriveCampaignStatus, NOT read from Airtable
    - getEnrollmentCountsByCampaign():
      - Fetches all CampaignEnrollments selecting only 'קמפיין' field
      - Returns Record<string, number> where key is campaignId, value is count
      - Returns empty object when no enrollments
      - Correctly accumulates multiple enrollments for the same campaign
    - deriveCampaignStatus(eventDateISO: string): Campaign['status']
      - Compares event_date to current date using new Date()
      - 'future': now < eventDate
      - 'active': eventDate <= now < dayAfterEvent
      - 'ended': now >= dayAfterEvent
      - Export this function (it's used in tests and potentially in UI)
    - Update getCampaigns() to use deriveCampaignStatus() instead of reading 'סטטוס' from Airtable
      (removes the r.fields['סטטוס'] read — status is computed, not stored)
  </behavior>
  <action>
    Add to src/lib/airtable/campaigns.ts (keep existing getCampaigns and getCampaignById, modify getCampaigns to use deriveCampaignStatus):

    1. Export deriveCampaignStatus function (implementation from RESEARCH.md Code Examples section).

    2. Modify getCampaigns() map to call deriveCampaignStatus(r.fields['תאריך אירוע'] as string) instead of r.fields['סטטוס'].

    3. Add createCampaign({ campaign_name, event_date, event_time, description? }):
       - Call airtableBase('Campaigns').create([{ fields: { 'שם קמפיין': campaign_name, 'תאריך אירוע': event_date, 'שעת האירוע': event_time, 'תיאור': description ?? '' } }], { typecast: true })
       - Map returned record[0] to Campaign interface, using deriveCampaignStatus for status
       - Return the Campaign object

    4. Add getEnrollmentCountsByCampaign() → See RESEARCH.md Code Examples section for full implementation pattern.

    Run: npm test -- --testPathPattern="campaigns.test" — new describe blocks should go GREEN.
  </action>
  <verify>
    <automated>cd "D:\CLAUDE CODE HERE\MICAHL CRM" && npm test -- --testPathPattern="campaigns.test" 2>&1 | tail -25</automated>
  </verify>
  <done>All campaigns.test.ts tests GREEN including new createCampaign, getEnrollmentCountsByCampaign, and deriveCampaignStatus tests. Existing getCampaigns and getCampaignById tests still GREEN.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Create scheduled-messages.ts service</name>
  <files>src/lib/airtable/scheduled-messages.ts</files>
  <behavior>
    - getScheduledMessagesByCampaign(campaignId: string):
      - Returns ScheduledMessage[] for the given campaign
      - Uses FIND+ARRAYJOIN filterByFormula on 'קמפיין' linked field
      - Sorted by 'שליחה בשעה' ascending
    - upsertScheduledMessages(campaignId: string, slots: SlotData[]):
      - SlotData: { offset_label, message_content, send_at }
      - Skips slots where message_content.trim() === ''
      - Calls airtableBase('ScheduledMessages').create() with correct fields
      - Wraps campaignId in array: 'קמפיין': [campaignId]
      - Sets initial status to 'ממתינה' (Hebrew for pending)
      - Returns void
    - updateScheduledMessage(recordId: string, fields: { message_content?: string; send_at?: string }):
      - Calls airtableBase('ScheduledMessages').update(recordId, patchFields)
      - Only includes fields that are defined (partial update)
      - Hebrew field mapping: message_content → 'תוכן ההודעה', send_at → 'שליחה בשעה'
      - Returns void
  </behavior>
  <action>
    Create src/lib/airtable/scheduled-messages.ts:

    1. Add 'server-only' import at top.
    2. Import airtableBase from './client', ScheduledMessage from './types'.
    3. Define SlotData interface: { offset_label: ScheduledMessage['offset_label']; message_content: string; send_at: string }
    4. Implement getScheduledMessagesByCampaign(campaignId: string): use filterByFormula with FIND+ARRAYJOIN pattern, select sort by שליחה בשעה ascending, map records to ScheduledMessage.
    5. Implement upsertScheduledMessages per behavior spec above — see RESEARCH.md Pattern 6 for full implementation reference.
    6. Implement updateScheduledMessage per behavior spec above.

    Hebrew field name for offset label in Airtable is 'תזמון'. Map offset_label values to Hebrew display:
    - 'week_before' → 'שבוע לפני'
    - 'day_before' → 'יום לפני'
    - 'morning' → 'בוקר האירוע'
    - 'half_hour' → 'חצי שעה לפני'

    Run: npm test -- --testPathPattern="scheduled-messages.test" — all tests go GREEN.
  </action>
  <verify>
    <automated>cd "D:\CLAUDE CODE HERE\MICAHL CRM" && npm test -- --testPathPattern="scheduled-messages.test" 2>&1 | tail -25</automated>
  </verify>
  <done>All scheduled-messages.test.ts tests GREEN. upsertScheduledMessages correctly skips empty slots and wraps campaignId in array. updateScheduledMessage correctly patches Hebrew field names.</done>
</task>

</tasks>

<verification>
Full test suite after Plan 02:
- npm test passes with all new tests GREEN
- No regressions in Phase 2 tests (contacts.test.ts, phone.test.ts)
</verification>

<success_criteria>
- campaigns.ts exports: getCampaigns, getCampaignById, createCampaign, getEnrollmentCountsByCampaign, deriveCampaignStatus
- scheduled-messages.ts exports: getScheduledMessagesByCampaign, upsertScheduledMessages, updateScheduledMessage
- All tests in campaigns.test.ts and scheduled-messages.test.ts GREEN
- getCampaigns() no longer reads 'סטטוס' from Airtable — status computed via deriveCampaignStatus
</success_criteria>

<output>
After completion, create .planning/phases/03-campaign-management/03-02-SUMMARY.md
</output>
