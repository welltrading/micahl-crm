---
phase: 03-campaign-management
plan: 05
type: execute
wave: 5
depends_on:
  - 03-04
files_modified:
  - src/lib/airtable/scheduled-messages.ts
  - src/app/kampanim/actions.ts
  - src/lib/airtable/__tests__/scheduled-messages.test.ts
autonomous: true
gap_closure: true
requirements:
  - CAMP-03
  - CAMP-06

must_haves:
  truths:
    - "Every Airtable write to ScheduledMessages includes שליחה בשעה with a UTC ISO8601 value"
    - "upsertScheduledMessages writes שליחה בשעה in both create and update paths"
    - "createScheduledMessage writes שליחה בשעה"
    - "updateScheduledMessage writes שליחה בשעה when send_date or send_time changes"
    - "updateMessageTimeAction extracts HH:MM from ISO string before writing שעת שליחה and correctly writes שליחה בשעה"
    - "Tests verify שליחה בשעה is populated with the correct UTC value in all write paths"
  artifacts:
    - path: "src/lib/airtable/scheduled-messages.ts"
      provides: "All write functions include שליחה בשעה field"
      contains: "שליחה בשעה"
    - path: "src/app/kampanim/actions.ts"
      provides: "updateMessageTimeAction writes correct HH:MM to שעת שליחה and UTC to שליחה בשעה"
      contains: "שליחה בשעה"
    - path: "src/lib/airtable/__tests__/scheduled-messages.test.ts"
      provides: "Tests assert שליחה בשעה populated in create and update mocks"
      contains: "שליחה בשעה"
  key_links:
    - from: "src/lib/airtable/scheduled-messages.ts"
      to: "src/lib/airtable/timezone.ts"
      via: "localIsraelToUTC imported and called before every Airtable write"
      pattern: "localIsraelToUTC"
    - from: "src/app/kampanim/actions.ts"
      to: "src/lib/airtable/timezone.ts"
      via: "localIsraelToUTC called in updateMessageTimeAction to compute UTC from date+time"
      pattern: "localIsraelToUTC"
---

<objective>
Fix the critical data pipeline gap: every write to ScheduledMessages must populate the `שליחה בשעה` (UTC DateTime) field that the Bree scheduler queries with `IS_BEFORE({שליחה בשעה}, NOW())`. Without this field, messages created through the UI will never be sent.

Purpose: Close CAMP-03 blocker. The timezone utility is correct and tested — the pipeline from UI → Airtable is broken because the UTC field is never written. This plan wires the existing `localIsraelToUTC` function into every Airtable write path.

Output: `scheduled-messages.ts` and `actions.ts` write `שליחה בשעה` on every create/update. Tests verify the UTC value is present.
</objective>

<execution_context>
@C:/Users/londi/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/londi/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/03-campaign-management/03-CONTEXT.md
@.planning/phases/03-campaign-management/03-04-SUMMARY.md

<interfaces>
<!-- From src/lib/airtable/timezone.ts — server-only, safe to import in scheduled-messages.ts -->
```typescript
// Already marked 'server-only' — importable in any server module

export function localIsraelToUTC(dateISO: string, timeHHMM: string): string;
// dateISO: 'YYYY-MM-DD' (Israel local date)
// timeHHMM: 'HH:MM' (24-hour Israel wall clock)
// returns: UTC ISO8601, e.g. '2026-04-07T06:00:00.000Z' for 2026-04-07 09:00 IDT
```

<!-- From src/lib/airtable/scheduled-messages.ts (current state) -->
```typescript
export interface SlotData {
  slot_index: number;    // 1–4
  recordId?: string;     // existing Airtable record ID (for update)
  title: string;
  message_content: string;
  send_date: string;     // YYYY-MM-DD Israel local
  send_time: string;     // HH:MM Israel local
}

// Current sharedFields (MISSING שליחה בשעה):
const sharedFields = {
  'כותרת': slot.title,
  'תוכן ההודעה': slot.message_content,
  'תאריך שליחה': slot.send_date,
  'שעת שליחה': slot.send_time,
  // ← שליחה בשעה must be added here
};
```

<!-- Current broken updateMessageTimeAction in actions.ts (line 129) -->
```typescript
// BUG: passes full ISO string "2026-04-07T06:00:00.000Z" to send_time field expecting HH:MM
await updateScheduledMessage(recordId, { send_time: send_at });
// send_at is a UTC ISO8601 string — this corrupts שעת שליחה and never writes שליחה בשעה
```

<!-- What Bree scheduler queries (from src/lib/airtable/scheduler-services.ts) -->
// filterByFormula: "AND({סטטוס}='ממתינה', IS_BEFORE({שליחה בשעה}, NOW()))"
// If שליחה בשעה is null/empty → IS_BEFORE returns false → message never triggered
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Write שליחה בשעה in all scheduled-messages.ts write paths</name>
  <files>src/lib/airtable/scheduled-messages.ts, src/lib/airtable/__tests__/scheduled-messages.test.ts</files>
  <behavior>
    - Test: upsertScheduledMessages create path — mockCreate called with fields containing 'שליחה בשעה': '2026-04-07T06:00:00.000Z' (for send_date '2026-04-07', send_time '09:00' IDT)
    - Test: upsertScheduledMessages update path — mockUpdate called with fields containing 'שליחה בשעה'
    - Test: createScheduledMessage — mockCreate called with fields containing 'שליחה בשעה'
    - Test: updateScheduledMessage with send_date + send_time — mockUpdate called with 'שליחה בשעה'
    - Test: updateScheduledMessage with only message_content (no date/time) — 'שליחה בשעה' NOT included in update (no date/time to compute from)
  </behavior>
  <action>
    Step 1 — Update tests first (RED):

    In `src/lib/airtable/__tests__/scheduled-messages.test.ts`:

    1. Import `localIsraelToUTC` from timezone.ts is NOT needed in tests — just verify the field value directly using known inputs.

    2. Add to the `upsertScheduledMessages` create-path test ("wraps campaignId in array"):
       - Assert: `mockCreate.mock.calls[0][0][0].fields['שליחה בשעה']` equals `'2026-04-07T06:00:00.000Z'`
       - BASE_SLOT has send_date '2026-04-07', send_time '09:00' — IDT (UTC+3) → UTC is 06:00 → `'2026-04-07T06:00:00.000Z'`
       - Note: test runs in Node — Intl.DateTimeFormat with 'Asia/Jerusalem' works correctly in Node 18+

    3. Add to the `updates existing record when slot_index already exists` test:
       - Assert: `mockUpdate.mock.calls[0][0][0].fields['שליחה בשעה']` is truthy (non-empty string)

    4. Add a new `updateScheduledMessage` test:
       - `it('writes שליחה בשעה when send_date and send_time both provided')`
       - Call `updateScheduledMessage('recMsg1', { send_date: '2026-04-07', send_time: '09:00' })`
       - Assert: `mockUpdate.mock.calls[0][1]['שליחה בשעה']` equals `'2026-04-07T06:00:00.000Z'`

    5. Add a new `updateScheduledMessage` test:
       - `it('does NOT write שליחה בשעה when only message_content is updated')`
       - Call `updateScheduledMessage('recMsg1', { message_content: 'New content' })`
       - Assert: `mockUpdate.mock.calls[0][1]` does NOT have key `'שליחה בשעה'`

    Step 2 — Fix scheduled-messages.ts (GREEN):

    In `src/lib/airtable/scheduled-messages.ts`:

    1. Add import at top: `import { localIsraelToUTC } from './timezone';`
       (timezone.ts is already 'server-only'; scheduled-messages.ts is also server-only — safe)

    2. In `upsertScheduledMessages`, update `sharedFields` to include UTC:
       ```typescript
       const utcSendAt = localIsraelToUTC(slot.send_date, slot.send_time);
       const sharedFields = {
         'כותרת': slot.title,
         'תוכן ההודעה': slot.message_content,
         'תאריך שליחה': slot.send_date,
         'שעת שליחה': slot.send_time,
         'שליחה בשעה': utcSendAt,
       };
       ```
       Place the `localIsraelToUTC` call BEFORE the sharedFields object, inside the `for (const slot of filled)` loop.

    3. In `createScheduledMessage`, add UTC field to the create call:
       ```typescript
       const utcSendAt = localIsraelToUTC(slot.send_date, slot.send_time);
       const created = await airtableBase('הודעות מתוזמנות').create({
         'קמפיין': [campaignId],
         'כותרת': slot.title,
         'תוכן ההודעה': slot.message_content,
         'תאריך שליחה': slot.send_date,
         'שעת שליחה': slot.send_time,
         'שליחה בשעה': utcSendAt,
         'מספר הודעה': String(slot.slot_index),
         'סטטוס': 'ממתינה',
       });
       ```

    4. In `updateScheduledMessage`, compute UTC only when both send_date and send_time are provided:
       ```typescript
       if (fields.send_date !== undefined) update['תאריך שליחה'] = fields.send_date;
       if (fields.send_time !== undefined) update['שעת שליחה'] = fields.send_time;
       // Write UTC only when we have both date and time to compute from
       if (fields.send_date !== undefined && fields.send_time !== undefined) {
         update['שליחה בשעה'] = localIsraelToUTC(fields.send_date, fields.send_time);
       }
       ```

    Remove the two debug `console.log` statements in `upsertScheduledMessages` (lines 54 and 61-62) — they are production noise flagged in the verification report.
  </action>
  <verify>
    <automated>cd "D:\CLAUDE CODE HERE\MICAHL CRM" && npm test -- --testPathPattern="scheduled-messages.test" 2>&1 | tail -20</automated>
  </verify>
  <done>All scheduled-messages tests pass. Tests include assertions that 'שליחה בשעה' is present with correct UTC value in create/update mock calls. No console.log noise in production code.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Fix updateMessageTimeAction data corruption bug</name>
  <files>src/app/kampanim/actions.ts</files>
  <behavior>
    - The action currently receives a full ISO8601 string (send_at) and writes it directly to שעת שליחה (HH:MM field) — data corruption
    - Fix: updateMessageTimeAction must accept send_date (YYYY-MM-DD) and send_time (HH:MM), then compute UTC via localIsraelToUTC and write both fields correctly
    - OR: if the caller already has an ISO UTC string, extract the Israel local HH:MM from it and also write שליחה בשעה
    - Simplest correct approach: change signature to accept send_date + send_time (Israel local), call localIsraelToUTC, write all three fields via updateScheduledMessage
  </behavior>
  <action>
    In `src/app/kampanim/actions.ts`:

    1. Add import: `import { localIsraelToUTC } from '@/lib/airtable/timezone';`
       (timezone.ts has 'server-only' — safe in a Server Action file)

    2. Change `updateMessageTimeAction` signature and implementation:

    ```typescript
    export async function updateMessageTimeAction(
      recordId: string,
      send_date: string,   // YYYY-MM-DD Israel local
      send_time: string,   // HH:MM Israel local
    ): Promise<{ ok: true } | { error: string }> {
      try {
        if (!recordId) return { error: 'recordId is required' };
        if (!send_date) return { error: 'send_date is required' };
        if (!send_time) return { error: 'send_time is required' };

        // Validate date format YYYY-MM-DD
        if (!/^\d{4}-\d{2}-\d{2}$/.test(send_date)) {
          return { error: 'send_date must be YYYY-MM-DD' };
        }
        // Validate time format HH:MM
        if (!/^\d{2}:\d{2}$/.test(send_time)) {
          return { error: 'send_time must be HH:MM' };
        }

        await updateScheduledMessage(recordId, { send_date, send_time });
        // שליחה בשעה is now written by updateScheduledMessage when both date+time provided (Task 1)
        return { ok: true };
      } catch (err) {
        console.error('updateMessageTimeAction error:', err);
        return { error: 'שגיאה בעדכון זמן ההודעה. נסי שנית.' };
      }
    }
    ```

    Note: The `localIsraelToUTC` import is added but the UTC computation is now handled inside `updateScheduledMessage` (Task 1). The import is not needed in actions.ts directly — remove it if unused after the refactor. The key fix is changing the signature from `(recordId, send_at: string)` to `(recordId, send_date: string, send_time: string)` so callers pass Israel local values, not ISO UTC strings.

    3. Search CampaignSheet.tsx for any call to `updateMessageTimeAction` — update callers to pass (recordId, send_date, send_time) instead of (recordId, isoString). If no callers exist yet (the action may not have been wired in Plan 04 since saveMessagesAction handles saves), verify with grep and skip if unused.
  </action>
  <verify>
    <automated>cd "D:\CLAUDE CODE HERE\MICAHL CRM" && npx tsc --noEmit --project tsconfig.json 2>&1 | grep -E "error TS|updateMessageTimeAction" | head -20</automated>
  </verify>
  <done>updateMessageTimeAction signature changed to (recordId, send_date, send_time). TypeScript compiles without errors. No ISO string passed to שעת שליחה field anywhere in actions.ts. Full test suite passes: npm test returns green.</done>
</task>

</tasks>

<verification>
After both tasks complete:
- npm test -- --testPathPattern="scheduled-messages.test" — all pass including new שליחה בשעה assertions
- npm test — full suite green (33+ tests)
- npx tsc --noEmit — no TypeScript errors
- Grep check: grep -r "שליחה בשעה" src/lib/airtable/scheduled-messages.ts — appears in create and update paths
- Grep check: grep -r "שליחה בשעה" src/lib/airtable/__tests__/scheduled-messages.test.ts — appears in test assertions
</verification>

<success_criteria>
- 'שליחה בשעה' written in upsertScheduledMessages (both create and update paths)
- 'שליחה בשעה' written in createScheduledMessage
- 'שליחה בשעה' written in updateScheduledMessage when both send_date and send_time provided
- updateMessageTimeAction no longer passes ISO string to שעת שליחה
- Tests verify UTC value correctness with known test input (2026-04-07 09:00 IDT → 2026-04-07T06:00:00.000Z)
- Full npm test suite passes
</success_criteria>

<output>
After completion, create `.planning/phases/03-campaign-management/03-05-SUMMARY.md`
</output>
