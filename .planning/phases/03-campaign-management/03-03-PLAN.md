---
phase: 03-campaign-management
plan: 03
type: execute
wave: 3
depends_on:
  - 03-02
files_modified:
  - src/app/kampanim/page.tsx
  - src/app/kampanim/actions.ts
  - src/components/campaigns/CampaignsPageClient.tsx
  - src/components/campaigns/CampaignCard.tsx
  - src/components/campaigns/CreateCampaignModal.tsx
autonomous: true
requirements:
  - CAMP-01
  - CAMP-04
  - CAMP-05
  - UX-02
must_haves:
  truths:
    - "Michal sees a card grid of all campaigns with name, event date, status badge, enrollment count, and description"
    - "Status badge shows 'עתידי' (blue), 'פעיל' (green), or 'הסתיים' (gray) derived from event_date"
    - "Clicking 'צור קמפיין' opens a modal with Hebrew form fields including visual date input and time select"
    - "Submitting the modal creates the campaign and closes the modal"
    - "Empty state message shown when no campaigns exist"
    - "Page always shows fresh data (force-dynamic)"
  artifacts:
    - path: "src/app/kampanim/page.tsx"
      provides: "Server component fetching campaigns + enrollment counts"
      contains: "force-dynamic"
    - path: "src/app/kampanim/actions.ts"
      provides: "createCampaign Server Action"
      exports: ["createCampaignAction"]
    - path: "src/components/campaigns/CampaignsPageClient.tsx"
      provides: "Client component: card grid, modal trigger, sheet state"
      contains: "selectedCampaign"
    - path: "src/components/campaigns/CampaignCard.tsx"
      provides: "Single campaign card with status badge"
      contains: "CAMPAIGN_STATUS_BADGE"
    - path: "src/components/campaigns/CreateCampaignModal.tsx"
      provides: "Modal form for campaign creation"
      contains: "input type"
  key_links:
    - from: "src/app/kampanim/page.tsx"
      to: "src/lib/airtable/campaigns.ts"
      via: "getCampaigns() + getEnrollmentCountsByCampaign() in parallel Promise.all"
      pattern: "Promise.all"
    - from: "src/components/campaigns/CreateCampaignModal.tsx"
      to: "src/app/kampanim/actions.ts"
      via: "createCampaignAction Server Action called on form submit"
      pattern: "createCampaignAction"
    - from: "src/components/campaigns/CampaignsPageClient.tsx"
      to: "src/components/campaigns/CampaignCard.tsx"
      via: "maps campaigns array, passes campaign prop"
      pattern: "campaigns.map"
---

<objective>
Build the campaigns list page: server component fetching data, card grid UI, campaign creation modal with visual date/time inputs, and Server Action for creating campaigns. This is the entry point for campaign management.

Purpose: Michal needs to see all campaigns at a glance and create new ones without touching Airtable. Cards show status and enrollment counts. After creation the modal closes (Plan 04 wires up the Sheet auto-open).

Output: Fully functional campaigns list page at /kampanim with create flow.
</objective>

<execution_context>
@C:/Users/londi/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/londi/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/03-campaign-management/03-CONTEXT.md
@.planning/phases/03-campaign-management/03-RESEARCH.md
@.planning/phases/03-campaign-management/03-02-SUMMARY.md

<interfaces>
<!-- From src/lib/airtable/campaigns.ts (Plan 02) -->
```typescript
export async function getCampaigns(): Promise<Campaign[]>;
export async function getCampaignById(id: string): Promise<Campaign | null>;
export async function createCampaign(data: {
  campaign_name: string;
  event_date: string;   // YYYY-MM-DD from input
  event_time: string;   // HH:MM from select
  description?: string;
}): Promise<Campaign>;
export async function getEnrollmentCountsByCampaign(): Promise<Record<string, number>>;
export function deriveCampaignStatus(eventDateISO: string): Campaign['status'];
```

<!-- From src/lib/airtable/types.ts -->
```typescript
export interface Campaign {
  id: string;
  campaign_name: string;
  event_date: string;
  description?: string;
  status: 'future' | 'active' | 'ended';
  created_at: string;
}
```

<!-- Phase 2 established patterns -->
// Server component: src/app/anshei-kesher/page.tsx with export const dynamic = 'force-dynamic'
// Modal: src/components/contacts/AddContactModal.tsx uses Dialog from @base-ui/react/dialog
// Card component: src/components/ui/card.tsx
// RTL: dir="rtl" on <html>, use ms-* and ps-* logical properties for spacing

<!-- Phase 2 ContactsPageClient.tsx pattern (selectedContact state) -->
// const [selectedContact, setSelectedContact] = React.useState<Contact | null>(null);
// → replicate as selectedCampaign state in CampaignsPageClient
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Campaigns page server component and Server Action</name>
  <files>src/app/kampanim/page.tsx, src/app/kampanim/actions.ts</files>
  <action>
    Replace src/app/kampanim/page.tsx (currently a placeholder) with a server component:
    - Add 'use server' NOT needed — page.tsx is a server component by default
    - export const dynamic = 'force-dynamic' at top
    - Fetch [campaigns, enrollmentCounts] via Promise.all([getCampaigns(), getEnrollmentCountsByCampaign()])
    - Pass both as props to CampaignsPageClient
    - Import getCampaigns, getEnrollmentCountsByCampaign from '@/lib/airtable/campaigns'
    - Import CampaignsPageClient from '@/components/campaigns/CampaignsPageClient'

    Create src/app/kampanim/actions.ts:
    - 'use server' directive at top
    - Import createCampaign from '@/lib/airtable/campaigns'
    - Export async function createCampaignAction(formData: FormData): Promise<{ campaign: Campaign } | { error: string }>
      - Extract: campaign_name, event_date, event_time, description from formData
      - Validate: campaign_name required, event_date required, event_time required
      - Call createCampaign({ campaign_name, event_date, event_time, description })
      - Return { campaign } on success, { error: 'message' } on failure
      - Wrap in try/catch
  </action>
  <verify>
    <automated>cd "D:\CLAUDE CODE HERE\MICAHL CRM" && npx tsc --noEmit --project tsconfig.json 2>&1 | grep "kampanim" | head -10</automated>
  </verify>
  <done>page.tsx compiles without TypeScript errors. actions.ts exports createCampaignAction. No type errors in kampanim directory.</done>
</task>

<task type="auto">
  <name>Task 2: CampaignCard, CampaignsPageClient, and CreateCampaignModal</name>
  <files>
    src/components/campaigns/CampaignCard.tsx,
    src/components/campaigns/CampaignsPageClient.tsx,
    src/components/campaigns/CreateCampaignModal.tsx
  </files>
  <action>
    Create src/components/campaigns/CampaignCard.tsx:
    - 'use client' directive
    - Props: { campaign: Campaign; enrollmentCount: number; onClick: () => void }
    - Use Card, CardContent from '@/components/ui/card'
    - Status badge using CAMPAIGN_STATUS_BADGE and CAMPAIGN_STATUS_LABEL constants (from RESEARCH.md Code Examples):
      future: 'bg-blue-100 text-blue-700' label 'עתידי'
      active: 'bg-green-100 text-green-700' label 'פעיל'
      ended: 'bg-gray-100 text-gray-700' label 'הסתיים'
    - Display: campaign_name (bold), event_date formatted as Israeli date (toLocaleDateString('he-IL', {day:'numeric',month:'long',year:'numeric',timeZone:'Asia/Jerusalem'})), status badge, enrollmentCount (with Hebrew label "נרשמות"), description truncated at ~80 chars with ellipsis
    - Entire card is clickable — onClick triggers Sheet open
    - RTL layout using logical properties

    Create src/components/campaigns/CampaignsPageClient.tsx:
    - 'use client' directive
    - Props: { campaigns: Campaign[]; enrollmentCounts: Record<string, number> }
    - State: selectedCampaign: Campaign | null, isCreateModalOpen: boolean
    - Render:
      - Header row: right-aligned "קמפיינים" title + "צור קמפיין" button (triggers modal open)
      - Card grid: CSS grid, 3 columns on lg, 2 on md, 1 on sm — use Tailwind grid-cols-*
      - Map campaigns to CampaignCard with enrollmentCounts[c.id] ?? 0
      - Empty state when campaigns.length === 0: centered text "אין קמפיינים עדיין — צרי קמפיין ראשון"
    - CreateCampaignModal: open={isCreateModalOpen}, onOpenChange={setIsCreateModalOpen}, onCampaignCreated={(c) => { setSelectedCampaign(c); router.refresh(); }}
    - CampaignSheet (Plan 04 will fill this in): include a placeholder that renders null when selectedCampaign is null
    - Use useRouter from 'next/navigation' for router.refresh()

    Create src/components/campaigns/CreateCampaignModal.tsx:
    - 'use client' directive
    - Props: { open: boolean; onOpenChange: (o: boolean) => void; onCampaignCreated: (c: Campaign) => void }
    - Use Dialog from '@base-ui/react/dialog' — same import pattern as AddContactModal.tsx
    - Form fields (Hebrew labels, all RTL):
      - שם קמפיין: required text input
      - תאריך האירוע: native <input type="date"> — use className to style, dir="ltr" on the input itself (date values are LTR)
      - שעת האירוע: <select> with TIME_OPTIONS (every 30 min, 00:00 to 23:30 as array from RESEARCH.md Code Examples)
      - תיאור (optional): <textarea> rows=3
    - Submit: call createCampaignAction(formData), on success call onCampaignCreated(result.campaign) and onOpenChange(false)
    - Show inline error if result.error is set
    - Loading state: disable submit button while pending (use useTransition or isPending from useFormStatus)
    - Hebrew feminine submit button text: "צרי קמפיין"

    Note on date input: Use native <input type="date"> — do NOT use @base-ui/react/calendar (does not exist in v1.3.0 — see RESEARCH.md Critical Finding). The input returns YYYY-MM-DD string which is correct for the actions layer.
  </action>
  <verify>
    <automated>cd "D:\CLAUDE CODE HERE\MICAHL CRM" && npx tsc --noEmit --project tsconfig.json 2>&1 | grep "campaigns\|kampanim" | head -20</automated>
  </verify>
  <done>All 5 new files compile without TypeScript errors. /kampanim page loads in browser showing campaign cards or empty state. "צור קמפיין" button opens modal with date and time inputs.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>Campaigns list page at /kampanim: card grid, status badges, create modal with native date picker and time select, Server Action creating campaigns in Airtable.</what-built>
  <how-to-verify>
    1. Open http://localhost:3000/kampanim (run `npm run dev` if not running)
    2. Verify page loads in Hebrew RTL — title "קמפיינים" right-aligned, "צור קמפיין" button visible
    3. If no campaigns exist: empty state text "אין קמפיינים עדיין" is visible
    4. Click "צור קמפיין" — modal opens with Hebrew labels: שם קמפיין, תאריך האירוע, שעת האירוע, תיאור
    5. Fill in: שם "טסט קמפיין", תאריך any future date via date picker, שעה 19:00
    6. Click "צרי קמפיין" — modal closes, campaign card appears in grid
    7. Card shows: name, formatted Israeli date, status badge "עתידי" (blue), "0 נרשמות"
    8. Open Airtable Campaigns table — verify the new record exists with correct field values
  </how-to-verify>
  <resume-signal>Type "approved" or describe any issues seen</resume-signal>
</task>

</tasks>

<verification>
After human verify approval:
- npm test -- --testPathPattern="campaigns.test|scheduled-messages.test|timezone.test" all GREEN
- /kampanim page loads without error
- Campaign creation flow works end-to-end
</verification>

<success_criteria>
- /kampanim shows card grid with status badges and enrollment counts
- Creating a campaign via modal writes to Airtable and shows the card immediately
- Empty state works
- TypeScript compiles clean across all new files
</success_criteria>

<output>
After completion, create .planning/phases/03-campaign-management/03-03-SUMMARY.md
</output>
