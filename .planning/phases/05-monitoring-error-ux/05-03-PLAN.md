---
phase: 05-monitoring-error-ux
plan: 03
type: execute
wave: 2
depends_on:
  - 05-01
files_modified:
  - src/components/campaigns/CampaignSheet.tsx
autonomous: false
requirements:
  - MON-01
  - MON-02

must_haves:
  truths:
    - "CampaignSheet shows two tabs: 'הודעות' (existing messages) and 'יומן שליחות' (new log)"
    - "Switching to 'יומן שליחות' tab triggers lazy load of MessageLog entries for the campaign"
    - "Log table displays columns: שם מלא, טלפון (Israeli format), סטטוס, זמן שליחה, סיבת שגיאה"
    - "Error in סיבת שגיאה column shows friendly Hebrew via mapErrorToHebrew — not raw error code"
    - "'רק כשלונות' toggle filters table to only failed entries without making a second Airtable call"
    - "Opening Sheet for a different campaign resets log state (no stale data from previous campaign)"
    - "Loading state shown while log is fetching (spinner or skeleton)"
    - "Empty state shown when campaign has no log entries yet"
    - "Phone numbers in log table display in Israeli format 050-123-4567 using formatPhoneDisplay"
  artifacts:
    - path: "src/components/campaigns/CampaignSheet.tsx"
      provides: "tabbed layout with 'יומן שליחות' log tab, lazy load, failures toggle"
      contains: "יומן שליחות"
  key_links:
    - from: "CampaignSheet.tsx log tab useEffect"
      to: "getCampaignLogAction in actions.ts"
      via: "import + call on activeTab === 'log' && logEntries === null"
      pattern: "getCampaignLogAction"
    - from: "CampaignSheet.tsx error cell"
      to: "mapErrorToHebrew in message-log.ts"
      via: "import + call on entry.error_message"
      pattern: "mapErrorToHebrew"
    - from: "CampaignSheet.tsx phone cell"
      to: "formatPhoneDisplay in phone.ts"
      via: "import + call on entry.phone"
      pattern: "formatPhoneDisplay"
---

<objective>
Add the "יומן שליחות" (message log) tab to the CampaignSheet. Michal clicks the tab to see all send attempts for the campaign — name, phone, status, timestamp, and friendly Hebrew error if failed. A "רק כשלונות" toggle filters to failures only (satisfies MON-02 without a separate UI).

Purpose: Closes MON-01 (delivery log) and MON-02 (who didn't receive) in a single tabbed surface, reusing the existing Sheet without new modals or pages.
Output: CampaignSheet with tabbed layout, lazy-loaded log, failures toggle.
</objective>

<execution_context>
@C:/Users/londi/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/londi/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/05-monitoring-error-ux/05-CONTEXT.md
@.planning/phases/05-monitoring-error-ux/05-RESEARCH.md
@.planning/phases/05-monitoring-error-ux/05-01-SUMMARY.md

<interfaces>
<!-- Contracts delivered by plan 05-01 -->

From src/lib/airtable/message-log.ts (after 05-01):
```typescript
export interface MessageLogDisplayEntry {
  id: string;
  contact_id: string;
  full_name?: string;   // from Airtable lookup field (may be undefined if lookup not configured)
  phone?: string;       // from Airtable lookup field (may be undefined if lookup not configured)
  status: 'sent' | 'failed';
  logged_at: string;    // ISO string from Airtable createdTime
  error_message?: string;
}

export function mapErrorToHebrew(rawError: string | undefined): string
// '' if undefined, Hebrew friendly string otherwise
```

From src/app/kampanim/actions.ts (after 05-01):
```typescript
export async function getCampaignLogAction(
  campaignId: string
): Promise<{ entries: MessageLogDisplayEntry[] } | { error: string }>
```

From src/lib/airtable/phone.ts (existing):
```typescript
export function formatPhoneDisplay(phone: string): string
// 972501234567 → 050-123-4567 (Israeli display format)
```

From src/components/campaigns/CampaignSheet.tsx (existing — key state/type patterns):
```typescript
// STATUS_BADGE already defined (reuse for log status column):
const STATUS_BADGE: Record<string, { label: string; className: string }> = {
  pending: { label: 'ממתין', className: 'bg-yellow-100 text-yellow-800' },
  sending: { label: 'נשלח כעת', className: 'bg-blue-100 text-blue-800' },
  sent:    { label: 'נשלח', className: 'bg-green-100 text-green-800' },
  failed:  { label: 'נכשל', className: 'bg-red-100 text-red-800' },
};

// CampaignSheet receives:
interface CampaignSheetProps {
  campaign: Campaign | null;
  enrollmentCount: number;
  onClose: () => void;
  onDelete: () => void;
}
```

Lazy load pattern (from Phase 2 ContactDetailPanel — established pattern):
```typescript
// Two useEffects:
// 1. Reset log state when campaign changes (prevents stale data)
React.useEffect(() => {
  setLogEntries(null);
  setLogError(null);
}, [campaign?.id]);

// 2. Fetch only when log tab first opened
React.useEffect(() => {
  if (activeTab !== 'log' || !campaign) return;
  if (logEntries !== null) return; // already loaded
  let cancelled = false;
  setLogLoading(true);
  getCampaignLogAction(campaign.id).then((result) => {
    if (cancelled) return;
    setLogLoading(false);
    if ('error' in result) { setLogError(result.error); return; }
    setLogEntries(result.entries);
  });
  return () => { cancelled = true; };
}, [activeTab, campaign?.id]);
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add tab navigation and log state to CampaignSheet</name>
  <files>src/components/campaigns/CampaignSheet.tsx</files>
  <action>
    MODIFY src/components/campaigns/CampaignSheet.tsx.

    1. Add imports at top:
       ```typescript
       import { getCampaignLogAction } from '@/app/kampanim/actions';
       import { mapErrorToHebrew, type MessageLogDisplayEntry } from '@/lib/airtable/message-log';
       import { formatPhoneDisplay } from '@/lib/airtable/phone';
       ```

    2. Add new state variables inside the CampaignSheet component function (alongside existing slot/broadcast state):
       ```typescript
       const [activeTab, setActiveTab] = React.useState<'messages' | 'log'>('messages');
       const [logEntries, setLogEntries] = React.useState<MessageLogDisplayEntry[] | null>(null);
       const [logLoading, setLogLoading] = React.useState(false);
       const [logError, setLogError] = React.useState<string | null>(null);
       const [showFailuresOnly, setShowFailuresOnly] = React.useState(false);
       ```

    3. Add reset useEffect (prevents stale data when opening different campaign):
       ```typescript
       React.useEffect(() => {
         setActiveTab('messages');
         setLogEntries(null);
         setLogError(null);
         setLogLoading(false);
         setShowFailuresOnly(false);
       }, [campaign?.id]);
       ```

    4. Add lazy-load useEffect:
       ```typescript
       React.useEffect(() => {
         if (activeTab !== 'log' || !campaign) return;
         if (logEntries !== null) return;
         let cancelled = false;
         setLogLoading(true);
         getCampaignLogAction(campaign.id).then((result) => {
           if (cancelled) return;
           setLogLoading(false);
           if ('error' in result) { setLogError(result.error); return; }
           setLogEntries(result.entries);
         });
         return () => { cancelled = true; };
       }, [activeTab, campaign?.id]);
       ```

    5. Add tab navigation bar in the SheetContent, immediately after the SheetHeader closing tag and before the existing messages content. Use pill-style tabs:
       ```typescript
       {/* Tab navigation */}
       <div className="flex gap-1 border-b pb-0 mb-4">
         <button
           className={`px-4 py-2 text-sm font-medium border-b-2 transition-colors ${activeTab === 'messages' ? 'border-primary text-primary' : 'border-transparent text-muted-foreground hover:text-foreground'}`}
           onClick={() => setActiveTab('messages')}
         >
           הודעות
         </button>
         <button
           className={`px-4 py-2 text-sm font-medium border-b-2 transition-colors ${activeTab === 'log' ? 'border-primary text-primary' : 'border-transparent text-muted-foreground hover:text-foreground'}`}
           onClick={() => setActiveTab('log')}
         >
           יומן שליחות
         </button>
       </div>
       ```

    6. Wrap the existing messages content section in `{activeTab === 'messages' && (...)}`.

    7. Add log tab content section `{activeTab === 'log' && (...)}` — implement in Task 2.

    TypeScript must compile clean after this task. The log tab content can be a placeholder `<div>טוען...</div>` temporarily.
  </action>
  <verify>
    <automated>npx tsc --noEmit 2>&1 | head -20</automated>
  </verify>
  <done>CampaignSheet compiles clean. Two tabs render. Clicking "יומן שליחות" switches activeTab state. Existing "הודעות" tab still works normally. Log state resets on campaign change.</done>
</task>

<task type="auto">
  <name>Task 2: Implement log tab content — table, failures toggle, empty/loading/error states</name>
  <files>src/components/campaigns/CampaignSheet.tsx</files>
  <action>
    MODIFY src/components/campaigns/CampaignSheet.tsx — implement the log tab content section.

    Replace the placeholder from Task 1 with the full log tab UI:

    ```typescript
    {activeTab === 'log' && (
      <div className="flex flex-col gap-4">
        {/* Loading state */}
        {logLoading && (
          <div className="flex items-center justify-center py-12 text-muted-foreground text-sm">
            טוענת יומן שליחות...
          </div>
        )}

        {/* Error state */}
        {logError && (
          <div className="rounded-md border border-red-200 bg-red-50 px-4 py-3 text-sm text-red-700">
            {logError}
          </div>
        )}

        {/* Log content */}
        {logEntries !== null && !logLoading && (
          <>
            {/* Failures toggle */}
            <div className="flex items-center gap-2">
              <label className="flex items-center gap-2 text-sm cursor-pointer select-none">
                <input
                  type="checkbox"
                  checked={showFailuresOnly}
                  onChange={(e) => setShowFailuresOnly(e.target.checked)}
                  className="h-4 w-4"
                />
                רק כשלונות
              </label>
              <span className="text-xs text-muted-foreground">
                ({(showFailuresOnly ? logEntries.filter(e => e.status === 'failed') : logEntries).length} רשומות)
              </span>
            </div>

            {/* Empty state */}
            {logEntries.length === 0 ? (
              <div className="flex items-center justify-center py-12 text-muted-foreground text-sm">
                אין רשומות יומן עדיין לקמפיין זה
              </div>
            ) : (
              /* Log table */
              <div className="overflow-x-auto">
                <table className="w-full text-sm">
                  <thead>
                    <tr className="border-b text-muted-foreground">
                      <th className="pb-2 text-right font-medium pe-3">שם מלא</th>
                      <th className="pb-2 text-right font-medium pe-3">טלפון</th>
                      <th className="pb-2 text-right font-medium pe-3">סטטוס</th>
                      <th className="pb-2 text-right font-medium pe-3">זמן שליחה</th>
                      <th className="pb-2 text-right font-medium">סיבת שגיאה</th>
                    </tr>
                  </thead>
                  <tbody>
                    {(showFailuresOnly ? logEntries.filter(e => e.status === 'failed') : logEntries).map((entry) => (
                      <tr key={entry.id} className="border-b last:border-0">
                        <td className="py-2 pe-3">{entry.full_name ?? '—'}</td>
                        <td className="py-2 pe-3" dir="ltr">
                          {entry.phone ? formatPhoneDisplay(entry.phone) : '—'}
                        </td>
                        <td className="py-2 pe-3">
                          {STATUS_BADGE[entry.status] ? (
                            <span className={`inline-flex items-center rounded-full px-2 py-0.5 text-xs font-medium ${STATUS_BADGE[entry.status].className}`}>
                              {STATUS_BADGE[entry.status].label}
                            </span>
                          ) : entry.status}
                        </td>
                        <td className="py-2 pe-3 text-muted-foreground tabular-nums" dir="ltr">
                          {entry.logged_at
                            ? new Date(entry.logged_at).toLocaleString('he-IL', {
                                timeZone: 'Asia/Jerusalem',
                                day: '2-digit',
                                month: '2-digit',
                                year: '2-digit',
                                hour: '2-digit',
                                minute: '2-digit',
                              })
                            : '—'}
                        </td>
                        <td className="py-2 text-red-600 text-xs max-w-[200px]">
                          {entry.error_message ? mapErrorToHebrew(entry.error_message) : ''}
                        </td>
                      </tr>
                    ))}
                  </tbody>
                </table>
              </div>
            )}
          </>
        )}
      </div>
    )}
    ```

    Use Tailwind 4 logical properties (`pe-*` not `pr-*`, `ps-*` not `pl-*`) throughout for RTL compatibility — same as the rest of the project.

    Phone column: `dir="ltr"` on the `<td>` — same pattern as contacts table from Phase 2.
    Timestamp column: `dir="ltr"` for consistent number display.

    STATUS_BADGE already imported via the existing map in the component — reuse it directly. 'sent' → 'נשלח', 'failed' → 'נכשל'.
  </action>
  <verify>
    <automated>npx tsc --noEmit 2>&1 | head -20</automated>
  </verify>
  <done>TypeScript compiles clean. Log tab renders table with all 5 columns. "רק כשלונות" toggle filters in-memory. Empty state shown when no entries. Loading state shown during fetch. Error state shown on fetch failure. Phone column uses dir=ltr. Status badge uses existing STATUS_BADGE map. Error column shows Hebrew via mapErrorToHebrew.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Human verify — יומן שליחות tab and failures toggle</name>
  <files>src/components/campaigns/CampaignSheet.tsx</files>
  <action>No automated action — human visually verifies the log tab built in Tasks 1 and 2.</action>
  <verify>
    <automated>MISSING — UI tab verification requires browser. See how-to-verify below.</automated>
  </verify>
  <done>Michal confirms: two tabs in CampaignSheet, log loads lazily on tab open, table shows 5 columns, failures toggle works without network call, Hebrew error messages display correctly.</done>
  <what-built>CampaignSheet with "יומן שליחות" tab. Lazy loads MessageLog entries on first tab open. Displays 5-column table. "רק כשלונות" toggle for failure-only view. All error messages in friendly Hebrew.</what-built>
  <how-to-verify>
    1. Start dev server: npm run dev (port 3001)
    2. Visit http://localhost:3001/kampanim
    3. Click on any campaign card — CampaignSheet slides in
    4. Verify two tabs are visible at the top: "הודעות" and "יומן שליחות"
    5. Click "הודעות" tab — existing message slot view still works normally
    6. Click "יומן שליחות" tab — loading indicator should briefly appear, then either:
       a. A table of send attempts (if campaign has MessageLog entries), OR
       b. "אין רשומות יומן עדיין לקמפיין זה" empty state
    7. If log entries exist: verify table shows שם מלא, טלפון (050-xxx format), סטטוס badge, זמן שליחה, סיבת שגיאה columns
    8. Check "רק כשלונות" checkbox — table should show only failed entries
    9. Uncheck "רק כשלונות" — all entries return
    10. Close sheet, open a different campaign — log tab should reset (no stale data from first campaign)
    11. If any failed entries exist: verify error text is in friendly Hebrew ("מספר הטלפון לא קיים..." / "גרין אפיאי מנותקת..." / etc.) — not raw error codes
  </how-to-verify>
  <resume-signal>Type "approved" or describe any issues found</resume-signal>
</task>

</tasks>

<verification>
- npx tsc --noEmit passes clean
- npx jest --no-coverage passes (no regressions in data layer tests)
- CampaignSheet has two tabs: "הודעות" (existing) and "יומן שליחות" (new)
- Log tab uses getCampaignLogAction (lazy, cancelled flag present)
- mapErrorToHebrew applied to error_message column
- formatPhoneDisplay applied to phone column
- "רק כשלונות" toggle filters already-loaded array (no second Airtable call)
- Campaign change resets all log state (useEffect on campaign?.id)
</verification>

<success_criteria>
- Two tabs visible in CampaignSheet
- Log loads lazily on first tab open, not on Sheet mount
- "רק כשלונות" toggle works without network call
- All 5 columns display correctly
- Phone numbers in Israeli format with dir=ltr
- Error messages in Hebrew via mapErrorToHebrew
- No stale data when switching campaigns
- TypeScript and full test suite green
</success_criteria>

<output>
After completion, create `.planning/phases/05-monitoring-error-ux/05-03-SUMMARY.md`
</output>
