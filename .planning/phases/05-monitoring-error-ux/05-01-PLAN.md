---
phase: 05-monitoring-error-ux
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/lib/airtable/message-log.ts
  - src/lib/airtable/__tests__/message-log.test.ts
  - src/app/kampanim/actions.ts
autonomous: true
requirements:
  - MON-01
  - UX-04

must_haves:
  truths:
    - "getMessageLogByCampaign(campaignId) returns MessageLogDisplayEntry[] sorted by logged_at desc"
    - "mapErrorToHebrew maps 401/notAuthorized errors to Hebrew 'גרין אפיאי מנותקת' string"
    - "mapErrorToHebrew maps 403/not-registered errors to Hebrew 'מספר הטלפון לא קיים בוואצאפ' string"
    - "mapErrorToHebrew maps network/timeout errors to Hebrew 'בעיית תקשורת זמנית' string"
    - "mapErrorToHebrew returns 'שגיאה לא ידועה' fallback for unknown error strings"
    - "getCampaignLogAction wraps getMessageLogByCampaign and returns { entries } on success, { error } on failure"
  artifacts:
    - path: "src/lib/airtable/message-log.ts"
      provides: "getMessageLogByCampaign, mapErrorToHebrew, MessageLogDisplayEntry type"
      exports: ["getMessageLogByCampaign", "mapErrorToHebrew", "MessageLogDisplayEntry"]
    - path: "src/lib/airtable/__tests__/message-log.test.ts"
      provides: "unit tests for read function and error mapper"
    - path: "src/app/kampanim/actions.ts"
      provides: "getCampaignLogAction server action"
      exports: ["getCampaignLogAction"]
  key_links:
    - from: "src/app/kampanim/actions.ts getCampaignLogAction"
      to: "src/lib/airtable/message-log.ts getMessageLogByCampaign"
      via: "direct import + await"
      pattern: "getMessageLogByCampaign"
    - from: "src/lib/airtable/message-log.ts"
      to: "Airtable MessageLog table"
      via: "FIND+ARRAYJOIN filterByFormula on 'קמפיין' linked field"
      pattern: "FIND.*ARRAYJOIN"
---

<objective>
Build the data layer for Phase 5 monitoring: a read function that fetches MessageLog entries per campaign, a pure error-mapping function that translates raw GREEN API errors into friendly Hebrew strings, and a server action thin wrapper for client use.

Purpose: The CampaignSheet log tab (plan 05-03) will depend on both getCampaignLogAction and mapErrorToHebrew. This plan delivers the tested contracts those tasks build against.
Output: getMessageLogByCampaign, mapErrorToHebrew (exported from message-log.ts), getCampaignLogAction (added to kampanim/actions.ts), full unit test suite.
</objective>

<execution_context>
@C:/Users/londi/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/londi/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-monitoring-error-ux/05-CONTEXT.md
@.planning/phases/05-monitoring-error-ux/05-RESEARCH.md
@.planning/phases/05-monitoring-error-ux/05-VALIDATION.md

<interfaces>
<!-- Key contracts executor needs — extracted from codebase -->

From src/lib/airtable/message-log.ts (current state — write-only):
```typescript
// Currently only exports:
export async function createMessageLogEntry(entry: MessageLogEntry): Promise<void>
// Hebrew field mapping documented in JSDoc:
//   'סטטוס'           — 'נשלחה' | 'נכשלה'
//   'הודעה מתוזמנת'   — linked: [scheduled_message_id]
//   'איש קשר'         — linked: [contact_id]
//   'קמפיין'          — linked: [campaign_id]
//   'תגובת GREEN API' — green_api_response
//   'הודעת שגיאה'     — error_message
// NOTE: NO 'logged_at' field is written — timestamp is Airtable auto-created (createdTime)
```

From src/lib/airtable/types.ts:
```typescript
export interface MessageLog {
  id: string;
  scheduled_message_id: string[];
  contact_id: string[];
  campaign_id: string[];
  status: 'sent' | 'failed';
  green_api_response?: string;
  error_message?: string;
  logged_at: string;  // NOTE: auto-created by Airtable, read via r._rawJson.createdTime
}
```

From src/lib/airtable/contacts.ts (FIND+ARRAYJOIN pattern — established Phase 2):
```typescript
// Linked record filter pattern (MUST use this — plain equality returns empty):
filterByFormula: `FIND("${campaignId}", ARRAYJOIN({קמפיין}))`
```

From src/app/kampanim/actions.ts (existing action pattern to follow):
```typescript
'use server';
export async function getCampaignMessagesAction(
  campaignId: string
): Promise<{ messages: ScheduledMessage[] } | { error: string }> {
  try {
    if (!campaignId) return { error: 'campaignId is required' };
    const messages = await getScheduledMessagesByCampaign(campaignId);
    return { messages };
  } catch (err) {
    console.error('getCampaignMessagesAction error:', err);
    return { error: 'שגיאה בטעינת ההודעות' };
  }
}
```

From src/lib/airtable/__tests__/scheduled-messages.test.ts (test mock pattern):
```typescript
const mockAll = jest.fn();
const mockSelect = jest.fn(() => ({ all: mockAll }));
const mockTable = jest.fn(() => ({ select: mockSelect, create: mockCreate }));
jest.mock('../client', () => ({ airtableBase: mockTable }));
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Write failing tests for getMessageLogByCampaign and mapErrorToHebrew</name>
  <files>src/lib/airtable/__tests__/message-log.test.ts</files>
  <behavior>
    - getMessageLogByCampaign: calls airtableBase('MessageLog').select with FIND+ARRAYJOIN filterByFormula containing campaignId, returns array of MessageLogDisplayEntry with id, contact_id, status ('sent' | 'failed'), logged_at from _rawJson.createdTime
    - getMessageLogByCampaign: maps 'נשלחה' → 'sent', 'נכשלה' → 'failed'
    - getMessageLogByCampaign: includes full_name and phone from r.fields if present (lookup fields)
    - getMessageLogByCampaign: includes error_message from 'הודעת שגיאה' field
    - mapErrorToHebrew('GREEN API error 401: notAuthorized') → contains 'גרין אפיאי מנותקת'
    - mapErrorToHebrew('GREEN API error 403: not registered') → contains 'מספר הטלפון לא קיים בוואצאפ'
    - mapErrorToHebrew('fetch failed timeout') → contains 'בעיית תקשורת זמנית'
    - mapErrorToHebrew('some random error xyz') → contains 'שגיאה לא ידועה'
    - mapErrorToHebrew(undefined) → returns empty string ''
    - getCampaignLogAction with valid campaignId returns { entries: MessageLogDisplayEntry[] }
    - getCampaignLogAction with empty campaignId returns { error: 'campaignId is required' }
  </behavior>
  <action>
    Create src/lib/airtable/__tests__/message-log.test.ts.

    Use the same mock pattern as scheduled-messages.test.ts:
    - mockAll, mockSelect, mockTable mock setup at top
    - jest.mock('../client', ...) before imports
    - Import getMessageLogByCampaign, mapErrorToHebrew from '../message-log'
    - Import getCampaignLogAction from '../../app/kampanim/actions' — mock message-log module for action tests

    For getMessageLogByCampaign tests: mock records with _rawJson.createdTime, fields['סטטוס'], fields['איש קשר'], fields['הודעת שגיאה'], fields['שם מלא'], fields['טלפון'].

    For mapErrorToHebrew tests: pure function, no mocks needed — call directly and assert string includes expected Hebrew substring.

    For getCampaignLogAction tests: jest.mock('../../../lib/airtable/message-log', ...) to stub getMessageLogByCampaign.

    Tests MUST fail before implementation (RED). Run: npx jest src/lib/airtable/__tests__/message-log.test.ts --no-coverage
    Expected: tests fail with "Cannot find module" or function-not-found errors.
  </action>
  <verify>
    <automated>npx jest "src/lib/airtable/__tests__/message-log.test.ts" --no-coverage 2>&1 | tail -20</automated>
  </verify>
  <done>Test file exists with all behavior cases. Tests fail RED (functions not yet exported).</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement getMessageLogByCampaign and mapErrorToHebrew — GREEN</name>
  <files>src/lib/airtable/message-log.ts, src/app/kampanim/actions.ts</files>
  <behavior>
    See Task 1 behavior list — implement until all tests pass GREEN.
  </behavior>
  <action>
    ADD to src/lib/airtable/message-log.ts (do NOT remove createMessageLogEntry):

    1. Export new interface MessageLogDisplayEntry:
    ```typescript
    export interface MessageLogDisplayEntry {
      id: string;
      contact_id: string;
      full_name?: string;   // Airtable lookup field 'שם מלא' on MessageLog (if configured)
      phone?: string;       // Airtable lookup field 'טלפון' on MessageLog (if configured)
      status: 'sent' | 'failed';
      logged_at: string;    // from (r as any)._rawJson?.createdTime ?? ''
      error_message?: string;
    }
    ```

    2. Export getMessageLogByCampaign:
    ```typescript
    export async function getMessageLogByCampaign(
      campaignId: string
    ): Promise<MessageLogDisplayEntry[]> {
      const records = await airtableBase('MessageLog')
        .select({
          filterByFormula: `FIND("${campaignId}", ARRAYJOIN({קמפיין}))`,
          sort: [{ field: 'Created', direction: 'desc' }],
        })
        .all();

      return records.map((r) => {
        const rawStatus = r.fields['סטטוס'] as string;
        return {
          id: r.id,
          contact_id: (r.fields['איש קשר'] as string[])?.[0] ?? '',
          full_name: r.fields['שם מלא'] as string | undefined,
          phone: r.fields['טלפון'] as string | undefined,
          status: rawStatus === 'נשלחה' ? 'sent' : 'failed',
          // eslint-disable-next-line @typescript-eslint/no-explicit-any
          logged_at: (r as any)._rawJson?.createdTime ?? '',
          error_message: r.fields['הודעת שגיאה'] as string | undefined,
        };
      });
    }
    ```
    NOTE: If 'Created' sort field throws Airtable error in tests, fall back to omitting sort and sorting client-side by logged_at after fetch.

    3. Export mapErrorToHebrew (per user-locked decisions):
    ```typescript
    export function mapErrorToHebrew(rawError: string | undefined): string {
      if (!rawError) return '';
      const lower = rawError.toLowerCase();
      if (lower.includes('401') || lower.includes('notauthorized') || lower.includes('unauthorized')) {
        return 'גרין אפיאי מנותקת — הודעות לא נשלחות, נא להתחבר מחדש בהגדרות';
      }
      if (lower.includes('403') || lower.includes('not registered') || lower.includes('notregistered')) {
        return 'מספר הטלפון לא קיים בוואצאפ — בדקי את המספר בכרטיסיית אנשי קשר';
      }
      if (lower.includes('timeout') || lower.includes('econnrefused') || lower.includes('network') || lower.includes('fetch')) {
        return 'בעיית תקשורת זמנית — ההודעה לא נשלחה, נסי שוב מאוחר יותר';
      }
      return 'שגיאה לא ידועה — פני לתמיכה אם הבעיה חוזרת';
    }
    ```

    ADD to src/app/kampanim/actions.ts (top of file, add to imports):
    ```typescript
    import { getMessageLogByCampaign, type MessageLogDisplayEntry } from '@/lib/airtable/message-log';
    ```

    ADD new export at end of actions.ts:
    ```typescript
    export async function getCampaignLogAction(
      campaignId: string
    ): Promise<{ entries: MessageLogDisplayEntry[] } | { error: string }> {
      try {
        if (!campaignId) return { error: 'campaignId is required' };
        const entries = await getMessageLogByCampaign(campaignId);
        return { entries };
      } catch (err) {
        console.error('getCampaignLogAction error:', err);
        return { error: 'שגיאה בטעינת יומן השליחות' };
      }
    }
    ```

    Run tests until GREEN: npx jest src/lib/airtable/__tests__/message-log.test.ts --no-coverage
  </action>
  <verify>
    <automated>npx jest "src/lib/airtable/__tests__/message-log.test.ts" --no-coverage</automated>
  </verify>
  <done>All tests pass GREEN. getMessageLogByCampaign, mapErrorToHebrew, getCampaignLogAction all exported. Full test suite still passes: npx jest --no-coverage</done>
</task>

</tasks>

<verification>
- npx jest src/lib/airtable/__tests__/message-log.test.ts --no-coverage — all tests GREEN
- npx jest --no-coverage — full suite GREEN (no regressions)
- src/lib/airtable/message-log.ts exports: getMessageLogByCampaign, mapErrorToHebrew, MessageLogDisplayEntry
- src/app/kampanim/actions.ts exports: getCampaignLogAction
- createMessageLogEntry still present and unchanged
</verification>

<success_criteria>
- All 10+ test cases pass (mapErrorToHebrew × 5, getMessageLogByCampaign × 3+, getCampaignLogAction × 2)
- TypeScript compiles with no errors (npx tsc --noEmit)
- FIND+ARRAYJOIN filter used (confirmed by test assertion on mock call args)
</success_criteria>

<output>
After completion, create `.planning/phases/05-monitoring-error-ux/05-01-SUMMARY.md`
</output>
