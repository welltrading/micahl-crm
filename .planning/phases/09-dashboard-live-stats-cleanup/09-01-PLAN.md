---
phase: 09-dashboard-live-stats-cleanup
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/lib/airtable/message-log.ts
  - src/lib/airtable/campaigns.ts
  - src/app/page.tsx
  - src/lib/airtable/__tests__/message-log.test.ts
  - src/lib/airtable/__tests__/campaigns.test.ts
autonomous: true
requirements: []

must_haves:
  truths:
    - "הדאשבורד מציג 4 כרטיסי סטטיסטיקה עם נתונים אמיתיים (לא '--')"
    - "כרטיס 'מתעניינות פעילות' מציג את הסה\"כ הגלובלי"
    - "כרטיס 'הודעות שנשלחו החודש' מציג ספירת sent בחודש הנוכחי"
    - "סטטוס GREEN API מוצג תמיד עם נקודה צבעונית"
    - "לוח קמפיינים מציג כל הקמפיינים עם נרשמות, מתעניינות ו-% המרה"
  artifacts:
    - path: "src/lib/airtable/message-log.ts"
      provides: "getMessagesSentThisMonth, getMessageLogSentCountsByCampaign"
      exports: ["getMessagesSentThisMonth", "getMessageLogSentCountsByCampaign"]
    - path: "src/lib/airtable/campaigns.ts"
      provides: "getInterestedCountsAllCampaigns batch function"
      exports: ["getInterestedCountsAllCampaigns"]
    - path: "src/app/page.tsx"
      provides: "Full dashboard with 4 stat cards + GREEN API + campaign grid"
      min_lines: 80
  key_links:
    - from: "src/app/page.tsx"
      to: "getMessagesSentThisMonth"
      via: "Promise.all import"
      pattern: "getMessagesSentThisMonth"
    - from: "src/app/page.tsx"
      to: "getInterestedCountsAllCampaigns"
      via: "Promise.all import"
      pattern: "getInterestedCountsAllCampaigns"
    - from: "src/app/page.tsx"
      to: "getGreenApiState"
      via: "Promise.all import"
      pattern: "getGreenApiState"
---

<objective>
Add three batch service functions and rewrite the dashboard page to display live data in 4 stat cards, a live GREEN API badge, and a full campaign grid.

Purpose: The dashboard currently shows "--" for messages and has a hardcoded gray GREEN API indicator. This plan replaces all hardcoded values with live Airtable data.
Output: Updated service layer (message-log.ts, campaigns.ts) + fully rewritten src/app/page.tsx.
</objective>

<execution_context>
@C:/Users/londi/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/londi/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-dashboard-live-stats-cleanup/09-CONTEXT.md
@.planning/phases/09-dashboard-live-stats-cleanup/09-RESEARCH.md

<interfaces>
<!-- Key existing functions — no exploration needed -->

From src/lib/airtable/campaigns.ts (existing, no changes needed):
```typescript
export async function getCampaigns(): Promise<Campaign[]>
export async function getInterestedCount(): Promise<number>           // global total
export async function getInterestedCountByCampaign(campaignId: string): Promise<number>
export async function getEnrollmentCountsByCampaign(): Promise<Record<string, number>>
```

From src/lib/airtable/green-api.ts:
```typescript
export async function getGreenApiState(): Promise<'authorized' | 'notAuthorized' | 'unknown'>
```

From src/lib/airtable/contacts.ts:
```typescript
export async function getContacts(): Promise<Contact[]>
```

From src/components/campaigns/CampaignCard.tsx (visual reference — do NOT modify):
```typescript
const CAMPAIGN_STATUS_BADGE: Record<Campaign['status'], string> = {
  future: 'bg-blue-100 text-blue-700',
  active: 'bg-green-100 text-green-700',
  ended: 'bg-gray-100 text-gray-700',
};
const CAMPAIGN_STATUS_LABEL: Record<Campaign['status'], string> = {
  future: 'עתידי', active: 'פעיל', ended: 'הסתיים',
};
```

From src/lib/airtable/types.ts (Campaign shape):
```typescript
export interface Campaign {
  id: string;
  campaign_name: string;
  event_date: string;   // ISO date string
  status: 'future' | 'active' | 'ended';
  description?: string;
}
```

Current src/app/page.tsx stat cards grid class (to be updated):
  `grid-cols-1 sm:grid-cols-3` → change to `grid-cols-2 sm:grid-cols-4`

Airtable table names:
  - MessageLog table: `יומן הודעות`
  - MessageLog status field: `סטטוס` — Hebrew values: `'נשלחה'` (sent), `'נכשלה'` (failed)
  - MessageLog campaign field: `קמפיין` (linked record, array of IDs)
  - Interested table: `מתעניינות`
  - Interested campaign field: `קמפיין` (linked record, array of IDs)

CRITICAL — filterByFormula pitfalls (from RESEARCH.md):
  - Use `{סטטוס} = 'נשלחה'` NOT `'sent'` — Airtable stores Hebrew values
  - Use `CREATED_TIME()` formula function for date range — there is no stored date field in MessageLog
  - UTC month boundaries: `new Date(Date.UTC(year, month, 1)).toISOString()`
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add three batch service functions</name>
  <files>
    src/lib/airtable/message-log.ts,
    src/lib/airtable/campaigns.ts,
    src/lib/airtable/__tests__/message-log.test.ts,
    src/lib/airtable/__tests__/campaigns.test.ts
  </files>
  <behavior>
    getMessagesSentThisMonth():
    - Returns count of MessageLog records where סטטוס = 'נשלחה' AND CREATED_TIME() falls in current calendar month (UTC boundaries)
    - Returns 0 when no records match
    - Filters by month using IS_AFTER/IS_BEFORE with Date.UTC boundaries (NOT local time)

    getMessageLogSentCountsByCampaign():
    - Returns Record&lt;campaignId, number&gt; — count of sent messages per campaign
    - Fetches only {סטטוס} = 'נשלחה' records, aggregates by קמפיין linked-record IDs
    - Returns empty object when no records exist

    getInterestedCountsAllCampaigns():
    - Returns Record&lt;campaignId, number&gt; — count of interested records per campaign
    - Single Airtable call to מתעניינות table, fields: ['קמפיין']
    - Returns empty object on error (no throw — dashboard degrades gracefully)
  </behavior>
  <action>
    **message-log.ts** — append two new exported async functions after existing exports:

    1. `getMessagesSentThisMonth(): Promise&lt;number&gt;`
       - Compute UTC month boundaries: `firstOfMonth = new Date(Date.UTC(year, month, 1)).toISOString()`, `firstOfNext = new Date(Date.UTC(year, month + 1, 1)).toISOString()`
       - filterByFormula: `AND({סטטוס} = 'נשלחה', IS_AFTER(CREATED_TIME(), '${firstOfMonth}'), IS_BEFORE(CREATED_TIME(), '${firstOfNext}'))`
       - fields: ['סטטוס']
       - Return `records.length`

    2. `getMessageLogSentCountsByCampaign(): Promise&lt;Record&lt;string, number&gt;&gt;`
       - filterByFormula: `{סטטוס} = 'נשלחה'`
       - fields: ['קמפיין']
       - Aggregate: for each record, cast `r.fields['קמפיין'] as string[] | undefined`, increment counts[id] for each id
       - Return counts (empty object `{}` if no records)

    **campaigns.ts** — append one new exported async function:

    3. `getInterestedCountsAllCampaigns(): Promise&lt;Record&lt;string, number&gt;&gt;`
       - Table: `מתעניינות`, fields: ['קמפיין']
       - Same aggregation pattern as above
       - Wrap in try/catch, return `{}` on error

    **message-log.test.ts** — add new describe block for the two new functions:
    - Mock `airtableBase` (follow existing mock pattern in the file)
    - Test `getMessagesSentThisMonth`: mock returns 2 records with סטטוס = 'נשלחה', expect result === 2
    - Test `getMessageLogSentCountsByCampaign`: mock returns records with קמפיין field arrays, expect correct aggregated map

    **campaigns.test.ts** — add describe block for `getInterestedCountsAllCampaigns`:
    - Test: mock returns records with קמפיין arrays → correct counts map
    - Test: mock throws → returns `{}`
  </action>
  <verify>
    <automated>cd "/d/CLAUDE CODE HERE/MICAHL CRM" && npm test -- --testPathPattern="message-log|campaigns" --passWithNoTests 2>&1 | tail -20</automated>
  </verify>
  <done>All three functions exported from their modules. New test describe blocks pass. npm test --testPathPattern="message-log|campaigns" exits 0.</done>
</task>

<task type="auto">
  <name>Task 2: Rewrite dashboard page.tsx with live stats + campaign grid</name>
  <files>src/app/page.tsx</files>
  <action>
    Fully rewrite `src/app/page.tsx` (keep `export const dynamic = 'force-dynamic'`):

    **Imports to add:**
    - `getGreenApiState` from `@/lib/airtable/green-api`
    - `getInterestedCount` from `@/lib/airtable/campaigns`
    - `getEnrollmentCountsByCampaign` from `@/lib/airtable/campaigns`
    - `getInterestedCountsAllCampaigns` from `@/lib/airtable/campaigns`
    - `getMessagesSentThisMonth` from `@/lib/airtable/message-log`
    - `getMessageLogSentCountsByCampaign` from `@/lib/airtable/message-log`
    - `deriveCampaignStatus` — check if exported from campaigns.ts or types; if not available, compute status from campaign.status field directly (it is already computed at read time per STATE.md decision)

    **Promise.all** — fetch all in parallel:
    ```
    const [campaigns, contacts, greenApiState, interestedCountGlobal, enrollmentCounts, interestedCountsMap, messagesSentThisMonth, sentCountsByCampaign] = await Promise.all([...])
    ```

    **Derived values:**
    - `activeCampaigns` = campaigns.filter(c => c.status === 'active' || c.status === 'future').length
    - `totalContacts` = contacts.length

    **4 stat cards** (grid: `grid-cols-2 sm:grid-cols-4 gap-4`):
    1. "קמפיינים פעילים/עתידיים" → `{activeCampaigns}`
    2. "אנשי קשר" → `{totalContacts}`
    3. "מתעניינות פעילות" → `{interestedCountGlobal}`
    4. "הודעות שנשלחו החודש" → `{messagesSentThisMonth}`

    **GREEN API badge** (always visible, same pattern as hagdarot/page.tsx):
    ```tsx
    const isConnected = greenApiState === 'authorized';
    const isUnknown = greenApiState === 'unknown';
    // Render:
    <Card>
      <CardContent className="flex items-center gap-2 pt-6">
        <div className={`w-2.5 h-2.5 rounded-full shrink-0 ${isUnknown ? 'bg-gray-400' : isConnected ? 'bg-green-500' : 'bg-red-500'}`} />
        <span className="text-sm font-medium">
          {`GREEN API: ${isUnknown ? 'סטטוס לא ידוע' : isConnected ? 'מחובר' : 'מנותק'}`}
        </span>
      </CardContent>
    </Card>
    ```

    **Campaign grid** — section header "קמפיינים" + `grid grid-cols-1 sm:grid-cols-2 lg:grid-cols-3 gap-4`:
    Each campaign card (inline JSX, do NOT modify CampaignCard.tsx):
    - Name + status badge (copy CAMPAIGN_STATUS_BADGE/LABEL constants inline)
    - Event date (use `toLocaleDateString('he-IL', { ... timeZone: 'Asia/Jerusalem' })`)
    - "נרשמות: X | מתעניינות: Y"
    - "% המרה: Z%" where Z = `Math.round((enrolled / interested) * 100)`, or "—" when interested === 0 or undefined
    - "הודעות שנשלחו: N"
    - No onClick (dashboard cards are display-only, not interactive)

    Conversion helper (inline function in page.tsx):
    ```typescript
    function conversionRate(enrolled: number, interested: number | undefined): string {
      if (!interested) return '—';
      return `${Math.round((enrolled / interested) * 100)}%`;
    }
    ```

    RTL: no dir overrides needed — html element already has dir=rtl from layout. Use `ms-*`/`ps-*` if adding inline spacing.
  </action>
  <verify>
    <automated>cd "/d/CLAUDE CODE HERE/MICAHL CRM" && npm run build 2>&1 | tail -20</automated>
  </verify>
  <done>`npm run build` exits 0 with no TypeScript errors. Manual browser check required before phase sign-off: run `npm run dev`, visit `/`, and confirm all 4 stat cards display numeric values (not "--"), the GREEN API badge shows a colored dot, and the campaign grid renders all campaigns.</done>
</task>

</tasks>

<verification>
- `npm test -- --testPathPattern="message-log|campaigns"` passes (new service function tests green)
- `npm run build` exits 0 (no TypeScript errors in page.tsx)
- All three new functions exported and importable
- No hardcoded "--" in page.tsx
- Manual browser check: visit `/` and confirm 4 stat cards show numeric values, GREEN API badge is visible, campaign grid renders
</verification>

<success_criteria>
- Four stat cards with live Airtable data (no "--")
- GREEN API badge always visible with correct color (green/red/gray)
- Campaign grid shows all campaigns with enrollment, interested, conversion %, and sent message counts
- No N+1 Airtable calls — all counts fetched in one batch call per data type
- `npm run build` and `npm test` both exit 0
</success_criteria>

<output>
After completion, create `.planning/phases/09-dashboard-live-stats-cleanup/09-01-SUMMARY.md`
</output>
