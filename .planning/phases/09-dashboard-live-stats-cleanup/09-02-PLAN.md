---
phase: 09-dashboard-live-stats-cleanup
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/lib/airtable/scheduled-messages.ts
  - src/lib/airtable/__tests__/scheduled-messages.test.ts
  - src/app/kampanim/actions.ts
autonomous: true
requirements: []

must_haves:
  truths:
    - "upsertScheduledMessages לא קיים בקוד המקור"
    - "updateMessageTimeAction לא קיים בקוד המקור"
    - "טסטים של upsertScheduledMessages הוסרו"
    - "טסטים של updateScheduledMessage עדיין עוברים"
  artifacts:
    - path: "src/lib/airtable/scheduled-messages.ts"
      provides: "scheduled messages service without upsertScheduledMessages"
    - path: "src/lib/airtable/__tests__/scheduled-messages.test.ts"
      provides: "tests for updateScheduledMessage only"
    - path: "src/app/kampanim/actions.ts"
      provides: "campaign actions without updateMessageTimeAction"
  key_links:
    - from: "src/lib/airtable/__tests__/scheduled-messages.test.ts"
      to: "src/lib/airtable/scheduled-messages.ts"
      via: "named import — must be updated after function removal"
      pattern: "import.*updateScheduledMessage.*from"
---

<objective>
Remove three dead code blocks that have no callers anywhere in the codebase.

Purpose: Dead code adds maintenance burden and misleads future readers into thinking these functions are part of the active API. All three have zero callers confirmed by codebase search.
Output: Three files with surgical deletions. No functionality change.
</objective>

<execution_context>
@C:/Users/londi/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/londi/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/09-dashboard-live-stats-cleanup/09-CONTEXT.md
@.planning/phases/09-dashboard-live-stats-cleanup/09-RESEARCH.md

<interfaces>
<!-- Dead code locations confirmed by RESEARCH.md direct file inspection -->

src/lib/airtable/scheduled-messages.ts:
  - DELETE: `upsertScheduledMessages` function (lines 38–102)
  - KEEP: `updateScheduledMessage` function and all other exports

src/lib/airtable/__tests__/scheduled-messages.test.ts:
  - DELETE: `describe('upsertScheduledMessages', ...)` block (lines 23–118)
  - UPDATE line 19 import:
      FROM: `import { upsertScheduledMessages, updateScheduledMessage } from '../scheduled-messages';`
      TO:   `import { updateScheduledMessage } from '../scheduled-messages';`
  - KEEP: `describe('updateScheduledMessage', ...)` block (lines 120–149, now renumbered)

src/app/kampanim/actions.ts:
  - DELETE: `updateMessageTimeAction` function (lines 129–155)
  - KEEP: all other exports (`createCampaignAction`, `broadcastAction`, etc.)
  - No import changes needed (function has no imports of its own that become orphaned)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Remove upsertScheduledMessages and its tests</name>
  <files>
    src/lib/airtable/scheduled-messages.ts,
    src/lib/airtable/__tests__/scheduled-messages.test.ts
  </files>
  <action>
    **Read both files first** to see exact line numbers before editing.

    **scheduled-messages.ts:**
    Delete the entire `upsertScheduledMessages` exported async function body. This is the function starting at approximately line 38. Remove from its JSDoc/comment (if any) through the closing `}` of the function. Keep all other functions intact: `createScheduledMessage`, `updateScheduledMessage`, `getScheduledMessages`, `resetStuckSendingMessages`, `markMessageSent`, etc.

    **scheduled-messages.test.ts:**
    1. Update the import on line 19:
       - Remove `upsertScheduledMessages` from the named import destructuring
       - Result: `import { updateScheduledMessage } from '../scheduled-messages';`
    2. Delete the entire `describe('upsertScheduledMessages', ...)` block including all nested `it`/`test` blocks and the closing `});`
    3. Leave the `describe('updateScheduledMessage', ...)` block completely untouched

    After editing, run `npm test -- --testPathPattern="scheduled-messages"` to confirm the remaining `updateScheduledMessage` tests still pass and the deleted tests are gone.
  </action>
  <verify>
    <automated>cd "/d/CLAUDE CODE HERE/MICAHL CRM" && npm test -- --testPathPattern="scheduled-messages" 2>&1 | tail -20</automated>
  </verify>
  <done>npm test --testPathPattern="scheduled-messages" exits 0. No test references `upsertScheduledMessages`. TypeScript build does not reference `upsertScheduledMessages`.</done>
</task>

<task type="auto">
  <name>Task 2: Remove updateMessageTimeAction from kampanim actions</name>
  <files>src/app/kampanim/actions.ts</files>
  <action>
    **Read src/app/kampanim/actions.ts first** to see current structure.

    Delete the `updateMessageTimeAction` exported async function (approximately lines 129–155). This includes:
    - Any JSDoc comment above it
    - The entire function from `export async function updateMessageTimeAction` through its closing `}`

    Verify no other code in actions.ts references `updateMessageTimeAction` as a local call.

    After deletion, check for any imports at the top of actions.ts that were used ONLY by `updateMessageTimeAction` — if any import is now unused, remove it. (Per RESEARCH.md, this function had no unique imports, so this is likely a no-op.)
  </action>
  <verify>
    <automated>cd "/d/CLAUDE CODE HERE/MICAHL CRM" && npm run build 2>&1 | tail -20</automated>
  </verify>
  <done>`npm run build` exits 0. `updateMessageTimeAction` does not appear in src/app/kampanim/actions.ts. No TypeScript errors.</done>
</task>

</tasks>

<verification>
- `npm test -- --testPathPattern="scheduled-messages"` passes (updateScheduledMessage tests green, upsertScheduledMessages tests absent)
- `npm run build` exits 0 (no TypeScript errors from deletions)
- Grep confirms zero occurrences of `upsertScheduledMessages` and `updateMessageTimeAction` in src/
</verification>

<success_criteria>
- Three dead code blocks removed with zero functional regressions
- `npm test` and `npm run build` both exit 0
- Codebase is smaller and contains no unreachable exported functions
</success_criteria>

<output>
After completion, create `.planning/phases/09-dashboard-live-stats-cleanup/09-02-SUMMARY.md`
</output>
